// Code generated by refactoring; DO NOT EDIT.

package web

const IndexHTML = `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#3544ee">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="apple-mobile-web-app-title" content="Gocord">
  <title>Gocord // Self-Hosted P2P Video Calls</title>
  <meta name="description" content="Ultra-low latency, peer-to-peer 60 FPS video calling. 100% self-hosted, standalone Go binary, zero server logs, and 256-bit E2EE.">

  <!-- Open Graph / Facebook / WhatsApp / Discord / Telegram -->
  <meta property="og:type" content="website">
  <meta property="og:site_name" content="Gocord">
  <meta property="og:title" content="Gocord // Self-Hosted P2P Video Calls">
  <meta property="og:description" content="Ultra-low latency, peer-to-peer 60 FPS video calling. 100% self-hosted, standalone Go binary, zero server logs, and 256-bit E2EE.">
  <meta property="og:image" content="/assets/favicon.png">
  <meta property="og:image:type" content="image/png">
  <meta property="og:image:width" content="1254">
  <meta property="og:image:height" content="1254">
  <meta property="og:image:alt" content="Gocord Mascot Logo">

  <!-- Twitter / X -->
  <meta name="twitter:card" content="summary">
  <meta name="twitter:title" content="Gocord // Self-Hosted P2P Video Calls">
  <meta name="twitter:description" content="Ultra-low latency, peer-to-peer 60 FPS video calling. 100% self-hosted, standalone Go binary, zero server logs, and 256-bit E2EE.">
  <meta name="twitter:image" content="/assets/favicon.png">
  <meta name="twitter:image:alt" content="Gocord Mascot Logo">

  <link rel="manifest" href="/assets/manifest.json">
  <link rel="apple-touch-icon" href="/assets/icon-192.png">
  <link rel="icon" href="/assets/favicon.png" type="image/png">
  <link rel="icon" href="/assets/favicon.ico" type="image/x-icon">
  <style>
:root {
  color-scheme: light dark;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  
  /* Neo-Brutalist / Maximalist Color Tokens */
  --bg-cobalt: #3544EE;
  --bg-cream: #F1EFEB;
  --bg-paper: #E7E4DC;
  --neon-lime: #C8FF00;
  --neon-yellow: #FFE600;
  --hot-orange: #FF5C00;
  --hot-pink: #FF3B81;
  --electric-cyan: #00F0FF;
  --ink-black: #0A0A0A;
  --pure-white: #FFFFFF;
  --gopher-blue: #63D0FF;
  
  /* Brutalist Strokes & Shadows */
  --border-thick: 3px solid var(--ink-black);
  --border-med: 2px solid var(--ink-black);
  --border-thin: 1.5px solid var(--ink-black);
  
  --shadow-sm: 3px 3px 0 var(--ink-black);
  --shadow-md: 4px 4px 0 var(--ink-black);
  --shadow-lg: 6px 6px 0 var(--ink-black);
  --shadow-xl: 8px 8px 0 var(--ink-black);
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

[hidden] {
  display: none !important;
}

.icon-inline {
  display: inline-block;
  vertical-align: middle;
  flex-shrink: 0;
}

.icon-btn {
  display: inline-block;
  flex-shrink: 0;
}

.icon-dock {
  display: inline-block;
  flex-shrink: 0;
}

.icon-input {
  display: inline-block;
  flex-shrink: 0;
  color: #64748B;
}

html, body {
  min-height: 100%;
  background: var(--bg-cobalt);
  color: var(--ink-black);
  overflow-x: hidden;
}

body {
  min-height: 100vh;
  min-height: 100dvh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

button, input, select {
  font: inherit;
  color: inherit;
}

button {
  cursor: pointer;
  border: none;
  background: none;
}

button:disabled {
  cursor: not-allowed;
  opacity: 0.6;
}

/* App Outer Shell */
.app-shell {
  width: 100%;
  min-height: 100vh;
  min-height: 100dvh;
  margin: 0 auto;
  padding: 24px 20px;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Main Brutalist Canvas Board Card */
.board-card {
  width: 100%;
  max-width: 1200px;
  background: var(--bg-cream);
  border: var(--border-thick);
  border-radius: 8px;
  box-shadow: var(--shadow-xl);
  padding: 24px 32px;
  display: flex;
  flex-direction: column;
  gap: 20px;
  position: relative;
  box-sizing: border-box;
}

.lobby-view, .friends-view {
  zoom: 1.3;
}

@media (max-width: 768px) {
  .lobby-view, .friends-view {
    zoom: 1.0;
  }
}

/* Fullscreen Call View Override */
.board-card.call-view {
  padding: 0;
  max-width: 100%;
  border-radius: 0;
  border: none;
  box-shadow: none;
}

/* ========================================== */
/* SCREEN 1: LOBBY & ROOM CREATOR             */
/* ========================================== */

.brutalist-nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
  padding-bottom: 12px;
  border-bottom: 2px dashed #D0CCC2;
}

.brand-mark {
  display: flex;
  align-items: center;
  gap: 8px;
}

/* Inline style attributes are blocked by the app's CSP (style-src 'self'). */
.brand-logo-img {
  width: 24px;
  height: 24px;
  object-fit: contain;
  border-radius: 50%;
}

.brand-logo {
  font-size: 1.25rem;
  font-weight: 800;
  letter-spacing: -0.03em;
  color: var(--ink-black);
  text-decoration: none;
}

.live-dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: var(--hot-pink);
  border: 1.5px solid var(--ink-black);
  display: inline-block;
  animation: pulse-dot 1.8s infinite ease-in-out;
}

@keyframes pulse-dot {
  0%, 100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.3); opacity: 0.7; }
}

.nav-links {
  display: flex;
  gap: 24px;
  align-items: center;
}

.nav-link {
  font-size: 0.82rem;
  font-weight: 800;
  letter-spacing: 0.05em;
  color: var(--ink-black);
  cursor: pointer;
  transition: color 0.15s ease;
}

.nav-link:hover {
  color: var(--bg-cobalt);
  text-decoration: underline;
}

.nav-link-btn {
  background: transparent;
  border: none;
  font-family: inherit;
  font-size: 0.82rem;
  font-weight: 800;
  letter-spacing: 0.05em;
  color: var(--ink-black);
  cursor: pointer;
  padding: 0;
  transition: color 0.15s ease;
}

.nav-link-btn:hover {
  color: var(--bg-cobalt);
  text-decoration: underline;
}

.nav-header-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

.nav-friends-mobile-btn {
  display: none;
  background: var(--pure-white);
  color: var(--ink-black);
  border: var(--border-thin);
  border-radius: 4px;
  font-family: inherit;
  font-size: 0.76rem;
  font-weight: 800;
  padding: 8px 12px;
  cursor: pointer;
  transition: transform 0.1s ease, background 0.15s ease;
}

.nav-friends-mobile-btn:hover {
  background: var(--bg-cream);
  color: var(--bg-cobalt);
}

.nav-cta-btn {
  background: var(--ink-black);
  color: var(--neon-lime);
  font-size: 0.82rem;
  font-weight: 800;
  padding: 8px 16px;
  border-radius: 4px;
  border: var(--border-thin);
  transition: transform 0.1s ease, box-shadow 0.1s ease;
}

.nav-cta-btn:hover {
  background: var(--bg-cobalt);
  color: var(--pure-white);
  box-shadow: 2px 2px 0 var(--ink-black);
}

/* Ticker Tape */
.ticker-tape {
  display: flex;
  align-items: center;
  gap: 8px;
  background: var(--neon-yellow);
  border: var(--border-med);
  border-radius: 4px;
  padding: 6px 12px;
  font-size: 0.75rem;
  font-weight: 800;
  letter-spacing: 0.04em;
  color: var(--ink-black);
}

.ticker-icon {
  font-size: 0.9rem;
}

/* Hero Split Area */
.hero-split {
  display: grid;
  grid-template-columns: 1.4fr 1fr;
  gap: 28px;
  width: 100%;
}

.hero-left {
  display: flex;
  flex-direction: column;
  gap: 16px;
  height: 100%;
}

.headline-block {
  display: flex;
  flex-direction: column;
  line-height: 0.92;
}

.hero-title-top {
  font-size: clamp(2.4rem, 5vw, 4rem);
  font-weight: 900;
  letter-spacing: -0.04em;
  color: var(--ink-black);
  margin: 0;
}

.hero-title-bottom-row {
  display: flex;
  align-items: center;
  gap: 12px;
}

.hero-title-bottom {
  font-size: clamp(2.4rem, 5vw, 4rem);
  font-weight: 900;
  letter-spacing: -0.04em;
  color: var(--bg-cobalt);
}

.star-sticker {
  background: var(--hot-orange);
  color: var(--pure-white);
  border: var(--border-med);
  border-radius: 4px;
  padding: 4px 10px;
  font-size: 0.85rem;
  font-weight: 800;
  letter-spacing: 0.02em;
  transform: rotate(-2deg);
}

.hero-subhead {
  font-size: 0.78rem;
  font-weight: 800;
  letter-spacing: 0.03em;
  color: var(--ink-black);
  line-height: 1.4;
}

/* Action Hub & Cards */
.lobby-action-hub {
  display: flex;
  flex-direction: column;
  gap: 12px;
  width: 100%;
  flex: 1;
}

.lobby-cta-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
  flex: 1;
}

.btn-create-room {
  background: var(--neon-lime);
  color: var(--ink-black);
  min-height: 72px;
  height: 100%;
  padding: 12px 18px;
  box-shadow: var(--shadow-sm);
  cursor: pointer;
  justify-content: flex-start;
  align-items: center;
  gap: 12px;
}

.btn-create-room:hover {
  background: #A3E635;
  color: var(--ink-black);
  box-shadow: var(--shadow-md);
}

.btn-friends-card {
  background: var(--bg-cobalt);
  color: var(--pure-white);
  min-height: 72px;
  height: 100%;
  padding: 12px 18px;
  box-shadow: var(--shadow-sm);
  cursor: pointer;
  justify-content: flex-start;
  align-items: center;
  gap: 12px;
}

.btn-friends-card:hover {
  background: var(--hot-orange);
  color: var(--pure-white);
  box-shadow: var(--shadow-md);
}

.btn-create-room .icon-btn,
.btn-friends-card .icon-btn {
  width: 24px;
  height: 24px;
  flex-shrink: 0;
}

.btn-text-col {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  text-align: left;
  line-height: 1.15;
}

.btn-primary-title {
  font-size: 1rem;
  font-weight: 900;
  letter-spacing: -0.01em;
}

.btn-sub-tag {
  font-size: 0.72rem;
  font-weight: 800;
  letter-spacing: 0.03em;
  opacity: 0.88;
}

.action-card {
  border: var(--border-thick);
  border-radius: 8px;
  padding: 16px;
  box-shadow: var(--shadow-md);
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.mobile-action-header {
  display: none;
}

.mobile-or-divider {
  display: none;
}

.join-card-compact {
  background: var(--bg-paper);
  border: var(--border-thick);
  border-radius: 8px;
  padding: 12px 14px;
  box-shadow: var(--shadow-md);
  flex-shrink: 0;
}

.create-card {
  background: var(--neon-lime);
}

.join-card {
  background: var(--bg-paper);
}

.action-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.action-tag {
  font-size: 0.78rem;
  font-weight: 800;
  letter-spacing: 0.04em;
  color: var(--ink-black);
}

.badge-pill {
  background: var(--ink-black);
  color: var(--neon-lime);
  font-size: 0.68rem;
  font-weight: 800;
  padding: 2px 6px;
  border-radius: 3px;
}

.action-desc {
  font-size: 0.82rem;
  font-weight: 700;
  color: var(--ink-black);
  line-height: 1.35;
}

/* Brutalist Buttons */
.btn-brutalist {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  border: var(--border-med);
  border-radius: 6px;
  font-weight: 800;
  letter-spacing: 0.03em;
  transition: transform 0.1s ease, box-shadow 0.1s ease;
  user-select: none;
}

.btn-brutalist:hover {
  transform: translate(-2px, -2px);
  box-shadow: var(--shadow-sm);
}

.btn-brutalist:active {
  transform: translate(2px, 2px);
  box-shadow: none;
}

.btn-create {
  width: 100%;
  height: 52px;
  background: var(--ink-black);
  color: var(--neon-lime);
  font-size: 0.95rem;
}

.btn-create:hover {
  background: var(--bg-cobalt);
  color: var(--pure-white);
}

.divider-strip {
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 0.72rem;
  font-weight: 800;
  letter-spacing: 0.08em;
  color: #71717A;
}

.join-input-row {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 10px;
}

.input-container {
  display: flex;
  align-items: center;
  gap: 8px;
  background: var(--pure-white);
  border: var(--border-med);
  border-radius: 6px;
  padding: 8px 12px;
}

.input-icon {
  font-size: 1rem;
}

.input-container input {
  width: 100%;
  border: none;
  outline: none;
  background: transparent;
  font-size: 0.85rem;
  font-weight: 700;
  color: var(--ink-black);
}

.input-container input::placeholder {
  color: #94A3B8;
  font-weight: 600;
}

.btn-join {
  background: var(--bg-cobalt);
  color: var(--pure-white);
  padding: 0 20px;
  font-size: 0.85rem;
}

.btn-join:hover {
  background: var(--hot-orange);
}

/* Hero Right Column (Graphic & Bento) */
.hero-right {
  background: var(--bg-paper);
  border: var(--border-thick);
  border-radius: 6px;
  padding: 20px;
  box-shadow: var(--shadow-lg);
  display: flex;
  flex-direction: column;
  gap: 16px;
  justify-content: space-between;
}

.hud-mini-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 0.75rem;
  font-weight: 800;
}

.rec-mini-pill {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: var(--ink-black);
  color: var(--pure-white);
  padding: 3px 8px;
  border-radius: 3px;
  font-size: 0.7rem;
}

.rec-mini-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--hot-pink);
}

.hud-stats-text {
  color: var(--ink-black);
}

.hero-logo-box {
  background: var(--bg-cobalt);
  border: var(--border-thick);
  border-radius: 10px;
  padding: 18px 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: var(--shadow-sm);
  overflow: hidden;
}

.hero-logo-img {
  width: 100%;
  max-width: 220px;
  height: auto;
  aspect-ratio: 1 / 1;
  object-fit: contain;
  border-radius: 8px;
  display: block;
  user-select: none;
}

.stickers-row {
  display: flex;
  gap: 10px;
  justify-content: space-between;
}

.sticker-chip {
  padding: 6px 12px;
  border: var(--border-med);
  border-radius: 4px;
  font-size: 0.75rem;
  font-weight: 800;
  letter-spacing: 0.02em;
}

.chip-lime {
  background: var(--neon-lime);
  color: var(--ink-black);
}

.chip-orange {
  background: var(--hot-orange);
  color: var(--pure-white);
}

.bento-mini-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}

.bento-mini-card {
  border: var(--border-med);
  border-radius: 6px;
  padding: 10px 12px;
  box-shadow: var(--shadow-sm);
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.bento-friends {
  background: var(--electric-lime);
  color: var(--ink-black);
  cursor: pointer;
  text-align: left;
  font-family: inherit;
  transition: transform 0.12s ease, box-shadow 0.12s ease, background 0.12s ease;
}

.bento-friends:hover {
  transform: translate(-2px, -2px);
  box-shadow: var(--shadow-md);
  background: #1de05f;
}

.bento-friends:active {
  transform: translate(2px, 2px);
  box-shadow: none;
}

.bento-coturn {
  background: var(--hot-orange);
  color: var(--pure-white);
}

.bento-zerologs {
  background: var(--hot-pink);
  color: var(--pure-white);
}

.bento-title {
  font-size: 0.85rem;
  font-weight: 900;
}

.bento-desc {
  font-size: 0.7rem;
  font-weight: 700;
  opacity: 0.9;
}

/* ========================================== */
/* SCREEN 2: PRE-CALL GREEN ROOM              */
/* ========================================== */

.precall-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-bottom: 12px;
  border-bottom: 2px dashed #D0CCC2;
}

.precall-header-actions {
  display: flex;
  align-items: center;
  gap: 10px;
}

.btn-nav-new-room {
  background: var(--ink-black);
  color: var(--neon-lime);
  padding: 6px 12px;
  font-size: 0.75rem;
  border-radius: 4px;
}

.btn-nav-new-room:hover {
  background: var(--bg-cobalt);
  color: var(--pure-white);
}

.precall-room-tag {
  background: var(--neon-yellow);
  border: var(--border-med);
  border-radius: 4px;
  padding: 4px 12px;
  font-size: 0.8rem;
  font-weight: 800;
}

.precall-split {
  display: grid;
  grid-template-columns: 1.3fr 1fr;
  gap: 24px;
  width: 100%;
}

.mirror-viewfinder-box {
  background: var(--ink-black);
  border: var(--border-thick);
  border-radius: 8px;
  padding: 16px;
  box-shadow: var(--shadow-lg);
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  gap: 16px;
  min-height: 440px;
}

.viewfinder-top-hud {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.viewfinder-tag {
  background: var(--neon-lime);
  color: var(--ink-black);
  font-size: 0.72rem;
  font-weight: 800;
  padding: 4px 8px;
  border-radius: 3px;
}

.vu-meter-pill {
  display: flex;
  align-items: center;
  gap: 6px;
  background: var(--bg-paper);
  border: var(--border-thin);
  border-radius: 4px;
  padding: 4px 8px;
}

.vu-icon {
  font-size: 0.75rem;
}

.vu-bars, .pip-vu-bars {
  display: flex;
  align-items: flex-end;
  gap: 2px;
  height: 14px;
}

.vu-bars .bar, .pip-vu-bars .bar {
  width: 3px;
  height: 4px;
  background: #3F3F46;
  border-radius: 1px;
  transition: height 0.05s ease, background 0.05s ease;
}

.vu-bars .bar.active, .pip-vu-bars .bar.active {
  background: var(--neon-lime);
}

.mirror-video-container {
  width: 100%;
  flex: 1;
  min-height: 280px;
  background: #12151D;
  border: var(--border-med);
  border-radius: 6px;
  position: relative;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
}

#previewVideo {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transform: scaleX(-1);
}

.mirror-fallback {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;
  color: var(--pure-white);
}

.avatar-disc-large {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  background: var(--bg-cobalt);
  border: var(--border-thick);
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 2.2rem;
}

.mirror-fallback-text {
  font-size: 0.85rem;
  font-weight: 800;
  letter-spacing: 0.04em;
}

.viewfinder-bottom-toggles {
  display: flex;
  gap: 8px;
  justify-content: center;
  flex-wrap: wrap;
}

.btn-toggle {
  padding: 8px 12px;
  border: var(--border-med);
  border-radius: 4px;
  font-size: 0.75rem;
  font-weight: 800;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  transition: all 0.1s ease;
}

.toggle-mic.active {
  background: var(--electric-cyan);
  color: var(--ink-black);
}

.toggle-mic:not(.active) {
  background: #27272A;
  color: #A1A1AA;
  border-color: #52525B;
}

.toggle-cam.active {
  background: var(--neon-lime);
  color: var(--ink-black);
}

.toggle-cam:not(.active) {
  background: #27272A;
  color: #A1A1AA;
  border-color: #52525B;
}

.toggle-noise.active {
  background: var(--hot-pink);
  color: var(--pure-white);
}

.toggle-noise:not(.active) {
  background: #27272A;
  color: #A1A1AA;
  border-color: #52525B;
}

/* Precall Right Actions Column */
.precall-actions-column {
  display: flex;
  flex-direction: column;
  gap: 16px;
  justify-content: space-between;
}

.invite-share-card {
  background: var(--hot-orange);
  color: var(--pure-white);
  border: var(--border-thick);
  border-radius: 6px;
  padding: 16px;
  box-shadow: var(--shadow-md);
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.invite-card-title {
  font-size: 0.95rem;
  font-weight: 900;
  letter-spacing: 0.03em;
}

.invite-card-sub {
  font-size: 0.72rem;
  font-weight: 700;
  opacity: 0.95;
  line-height: 1.3;
}

.copy-input-row {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 8px;
  margin-top: 4px;
}

.copy-input-row input {
  padding: 8px 10px;
  background: var(--bg-cream);
  border: var(--border-med);
  border-radius: 4px;
  font-size: 0.78rem;
  font-weight: 700;
  color: var(--ink-black);
}

.btn-copy {
  background: var(--neon-lime);
  color: var(--ink-black);
  padding: 0 14px;
  font-size: 0.78rem;
}

.btn-copy:hover {
  background: var(--pure-white);
}

.device-pickers-card {
  background: var(--bg-paper);
  border: var(--border-thick);
  border-radius: 6px;
  padding: 14px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.pickers-title {
  font-size: 0.75rem;
  font-weight: 800;
  letter-spacing: 0.04em;
  color: var(--ink-black);
}

.device-row {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.device-label {
  font-size: 0.7rem;
  font-weight: 800;
  color: var(--ink-black);
}

.device-select {
  width: 100%;
  padding: 8px 10px;
  background: var(--bg-cream);
  border: var(--border-med);
  border-radius: 4px;
  font-size: 0.78rem;
  font-weight: 700;
  color: var(--ink-black);
  outline: none;
}

.status-banner {
  font-size: 0.78rem;
  font-weight: 800;
  color: #52525B;
}

.error-banner {
  font-size: 0.78rem;
  font-weight: 800;
  color: #DC2626;
  background: #FEE2E2;
  border: 1.5px solid #DC2626;
  padding: 6px 10px;
  border-radius: 4px;
}

.btn-giant-join {
  width: 100%;
  height: 56px;
  background: var(--neon-lime);
  color: var(--ink-black);
  font-size: 1rem;
  border: var(--border-thick);
  box-shadow: var(--shadow-md);
}

.btn-giant-join:hover {
  background: var(--bg-cobalt);
  color: var(--pure-white);
}

.btn-create-another {
  width: 100%;
  height: 46px;
  background: var(--bg-paper);
  color: var(--ink-black);
  font-size: 0.88rem;
  border: var(--border-med);
  box-shadow: var(--shadow-sm);
}

.btn-create-another:hover {
  background: var(--neon-yellow);
  color: var(--ink-black);
}

/* ========================================== */
/* SCREEN 3: ACTIVE VIDEO CALL & BROADCAST    */
/* ========================================== */

.app-shell:has(#callView:not([hidden])) {
  padding: 0;
  max-width: 100%;
}

.call-view {
  padding: 0;
  overflow: hidden;
  background: var(--ink-black);
  max-width: 100% !important;
  border: none !important;
  border-radius: 0 !important;
  box-shadow: none !important;
}

.video-stage-board {
  display: flex;
  width: 100vw;
  min-height: 100vh;
  min-height: 100dvh;
  position: relative;
}

.remote-video-canvas {
  flex: 1;
  background: #0E1118;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  padding: 20px;
  position: relative;
}

/* Top Broadcast Camcorder HUD */
.camcorder-top-hud {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
  z-index: 10;
}

.hud-group-left {
  display: flex;
  gap: 10px;
  align-items: center;
}

.rec-live-tag {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: var(--hot-pink);
  color: var(--pure-white);
  border: var(--border-med);
  border-radius: 4px;
  padding: 4px 10px;
  font-size: 0.75rem;
  font-weight: 800;
  letter-spacing: 0.03em;
}

.rec-dot-pulsing {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--pure-white);
  animation: pulse-dot 1.4s infinite ease-in-out;
}

.room-id-tag {
  background: var(--neon-lime);
  color: var(--ink-black);
  border: var(--border-med);
  border-radius: 4px;
  padding: 4px 10px;
  font-size: 0.75rem;
  font-weight: 800;
  display: inline-flex;
  align-items: center;
  gap: 4px;
  transition: transform 0.1s ease;
}

.room-id-tag:hover {
  transform: scale(1.03);
}

.telemetry-pill {
  background: var(--bg-cream);
  color: var(--ink-black);
  border: var(--border-med);
  border-radius: 4px;
  padding: 4px 12px;
  font-size: 0.72rem;
  font-weight: 800;
  letter-spacing: 0.02em;
}

/* Video Stream Wrap */
.video-stream-wrap {
  flex: 1;
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 380px;
}

#remoteVideo {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: contain;
  border-radius: 6px;
}

.remote-placeholder-box {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  z-index: 5;
}

.remote-avatar-disc {
  width: 96px;
  height: 96px;
  border-radius: 50%;
  background: var(--hot-orange);
  border: var(--border-thick);
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 2.5rem;
  color: var(--pure-white);
  box-shadow: var(--shadow-sm);
}

.remote-peer-title {
  font-size: 1.1rem;
  font-weight: 900;
  letter-spacing: 0.04em;
  color: var(--pure-white);
}

.speaking-status-pill {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: var(--neon-lime);
  color: var(--ink-black);
  border: var(--border-med);
  border-radius: 20px;
  padding: 3px 12px;
  font-size: 0.72rem;
  font-weight: 800;
}

.btn-play-remote {
  position: absolute;
  z-index: 20;
  background: var(--hot-orange);
  color: var(--pure-white);
  border: var(--border-thick);
  border-radius: 30px;
  padding: 10px 20px;
  font-size: 0.85rem;
  font-weight: 800;
  box-shadow: var(--shadow-md);
}

.floating-pip-card {
  position: absolute;
  right: 20px;
  top: 20px;
  height: 140px;
  width: auto;
  aspect-ratio: 16 / 9;
  background: var(--ink-black);
  border: var(--border-thick);
  border-radius: 8px;
  box-shadow: var(--shadow-md);
  overflow: hidden;
  padding: 0;
  z-index: 50;
  cursor: grab;
  user-select: none;
  touch-action: none;
  transition: top 0.22s cubic-bezier(0.2, 0.9, 0.3, 1), left 0.22s cubic-bezier(0.2, 0.9, 0.3, 1), transform 0.15s ease;
}

.floating-pip-card.dragging {
  cursor: grabbing;
  transition: none !important;
  box-shadow: var(--shadow-xl);
  transform: scale(1.03);
}

.pip-viewport {
  width: 100%;
  height: 100%;
  background: #000;
  overflow: hidden;
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
}

#localVideo {
  width: 100%;
  height: 100%;
  object-fit: contain;
  transform: scaleX(-1);
  pointer-events: none;
}

#localVideo.no-mirror {
  transform: none;
}

/* Bottom Controls Dock */
.bottom-stage-controls {
  display: flex;
  justify-content: center;
  width: 100%;
  z-index: 15;
}

.tactile-control-dock {
  display: flex;
  gap: 8px;
  background: var(--bg-cream);
  border: var(--border-thick);
  border-radius: 8px;
  padding: 8px;
  box-shadow: var(--shadow-lg);
  flex-wrap: wrap;
  align-items: center;
  justify-content: center;
}

.dock-btn {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 8px 12px;
  border: var(--border-med);
  border-radius: 4px;
  font-size: 0.75rem;
  font-weight: 800;
  letter-spacing: 0.02em;
  transition: transform 0.1s ease, box-shadow 0.1s ease;
  user-select: none;
}

.dock-btn:hover {
  transform: translate(-1px, -1px);
  box-shadow: 2px 2px 0 var(--ink-black);
}

.dock-btn:active {
  transform: translate(1px, 1px);
  box-shadow: none;
}

.btn-dock-mic {
  background: var(--electric-cyan);
  color: var(--ink-black);
}
.btn-dock-mic[aria-pressed="true"] {
  background: #E4E4E7;
  color: #71717A;
  border-color: #A1A1AA;
}

.btn-dock-cam {
  background: var(--neon-lime);
  color: var(--ink-black);
}
.btn-dock-cam[aria-pressed="true"] {
  background: #E4E4E7;
  color: #71717A;
  border-color: #A1A1AA;
}

.btn-dock-share {
  background: var(--neon-yellow);
  color: var(--ink-black);
}
.btn-dock-share[aria-pressed="true"] {
  background: var(--hot-orange);
  color: var(--pure-white);
}

.btn-dock-fx {
  background: var(--hot-pink);
  color: var(--pure-white);
}
.btn-dock-fx[aria-pressed="true"] {
  background: var(--bg-cobalt);
}

.btn-dock-settings {
  background: var(--bg-paper);
  color: var(--ink-black);
}

.btn-dock-hangup {
  background: var(--hot-orange);
  color: var(--pure-white);
}
.btn-dock-hangup:hover {
  background: #DC2626;
}

/* Collapsible Media & Telemetry Drawer */
.telemetry-drawer-panel {
  width: 360px;
  background: var(--bg-cream);
  border-left: var(--border-thick);
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 18px;
  overflow-y: auto;
  transition: width 0.2s ease, padding 0.2s ease;
}

.telemetry-drawer-panel.collapsed {
  display: none !important;
}

.drawer-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.drawer-title {
  font-size: 0.88rem;
  font-weight: 900;
  letter-spacing: 0.04em;
  color: var(--ink-black);
}

.btn-drawer-collapse {
  background: var(--hot-pink);
  color: var(--pure-white);
  border: var(--border-thin);
  border-radius: 3px;
  padding: 4px 8px;
  font-size: 0.68rem;
  font-weight: 800;
}

.btn-drawer-collapse:hover {
  background: var(--ink-black);
}

.drawer-section {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.drawer-section-title {
  font-size: 0.72rem;
  font-weight: 900;
  letter-spacing: 0.04em;
  color: var(--ink-black);
}

.drawer-input-group {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.drawer-field-label {
  font-size: 0.68rem;
  font-weight: 800;
  color: var(--ink-black);
}

.drawer-select {
  width: 100%;
  padding: 6px 8px;
  background: var(--bg-paper);
  border: var(--border-med);
  border-radius: 4px;
  font-size: 0.75rem;
  font-weight: 700;
  color: var(--ink-black);
  outline: none;
}

.quality-pills-row {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 6px;
}

.btn-quality-pill {
  padding: 6px 4px;
  border: var(--border-med);
  border-radius: 4px;
  font-size: 0.7rem;
  font-weight: 800;
  background: var(--bg-paper);
  color: var(--ink-black);
  text-align: center;
  transition: all 0.1s ease;
}

.btn-quality-pill.active {
  background: var(--neon-lime);
  box-shadow: 2px 2px 0 var(--ink-black);
}

.metrics-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
}

.metric-tile {
  background: var(--bg-paper);
  border: var(--border-med);
  border-radius: 4px;
  padding: 8px;
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.metric-label {
  font-size: 0.65rem;
  font-weight: 800;
  color: var(--ink-black);
}

.metric-val {
  font-size: 0.85rem;
  font-weight: 900;
}

.val-blue { color: var(--bg-cobalt); }
.val-pink { color: var(--hot-pink); }
.val-dark { color: var(--ink-black); }
.val-orange { color: var(--hot-orange); }

/* ========================================== */
/* RESPONSIVE MOBILE BREAKPOINTS (< 768px)    */
/* ========================================== */

@media (max-width: 768px) {
  body {
    zoom: 1;
  }

  .app-shell {
    padding: max(12px, env(safe-area-inset-top)) max(10px, env(safe-area-inset-right)) max(12px, env(safe-area-inset-bottom)) max(10px, env(safe-area-inset-left));
    display: flex;
    justify-content: center;
    align-items: center;
    box-sizing: border-box;
  }

  .board-card {
    padding: 18px 14px;
    border: var(--border-thick);
    border-radius: 16px;
    box-shadow: var(--shadow-md);
    gap: 14px;
    box-sizing: border-box;
    width: 100%;
    max-width: 100%;
  }

  .board-card.lobby-view {
    min-height: auto;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
  }

  .brutalist-nav {
    padding-bottom: 10px;
  }

  .nav-links {
    display: none;
  }

  /* Remove contacts from mobile header as requested */
  .nav-friends-mobile-btn {
    display: none !important;
  }

  .brand-logo {
    font-size: 1.1rem;
  }

  .nav-cta-btn {
    padding: 6px 12px;
    font-size: 0.75rem;
    letter-spacing: 0.02em;
  }

  .ticker-tape {
    padding: 6px 10px;
    font-size: 0.68rem;
    font-weight: 800;
    gap: 6px;
    line-height: 1.2;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
    scrollbar-width: none;
  }

  .ticker-tape::-webkit-scrollbar {
    display: none;
  }

  .ticker-text {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .hero-split {
    grid-template-columns: 1fr;
    gap: 16px;
  }

  .hero-left {
    gap: 14px;
  }

  .headline-block {
    gap: 0;
  }

  .hero-title-top,
  .hero-title-bottom {
    font-size: 2.1rem;
    letter-spacing: -0.04em;
    line-height: 1;
  }

  .star-sticker {
    font-size: 0.72rem;
    padding: 3px 8px;
  }

  .hero-subhead {
    font-size: 0.72rem;
    line-height: 1.35;
    margin: 0;
    font-weight: 800;
    color: #334155;
  }

  .lobby-action-hub {
    gap: 12px;
    margin-top: 2px;
    flex: initial;
  }

  .lobby-cta-row {
    grid-template-columns: 1fr;
    gap: 10px;
    flex: initial;
  }

  .btn-create-room {
    min-height: 56px;
    height: auto;
    padding: 12px 16px;
    gap: 12px;
    border-radius: 8px;
    box-shadow: var(--shadow-sm);
  }

  .btn-friends-card {
    min-height: 50px;
    height: auto;
    padding: 10px 14px;
    gap: 10px;
    border-radius: 8px;
    box-shadow: var(--shadow-sm);
  }

  .btn-create-room .icon-btn,
  .btn-friends-card .icon-btn {
    width: 20px;
    height: 20px;
    flex-shrink: 0;
  }

  .btn-primary-title {
    font-size: 0.92rem;
  }

  .btn-sub-tag {
    font-size: 0.68rem;
  }

  .join-card-compact {
    padding: 14px;
    border: var(--border-thick);
    border-radius: 8px;
    background: var(--pure-white);
  }

  .mobile-action-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
  }

  .mobile-or-divider {
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.72rem;
    font-weight: 900;
    letter-spacing: 0.08em;
    color: #64748B;
    margin: 2px 0;
    text-transform: uppercase;
  }

  .join-input-row {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .input-container {
    padding: 10px 12px;
    border-radius: 6px;
  }

  .input-container input {
    font-size: 0.85rem;
  }

  .btn-join {
    width: 100%;
    padding: 12px;
    height: 46px;
    font-size: 0.85rem;
    border-radius: 6px;
    justify-content: center;
  }

  /* Fill empty space on mobile with rich, compact Bento specs and mascot showcase */
  .hero-right {
    display: flex !important;
    margin-top: 4px;
    padding: 16px;
    gap: 14px;
    border-radius: 10px;
  }

  .hero-logo-box {
    padding: 14px 12px;
  }

  .hero-logo-img {
    max-width: 150px;
  }

  .stickers-row {
    gap: 8px;
    flex-wrap: wrap;
  }

  .sticker-chip {
    padding: 4px 10px;
    font-size: 0.7rem;
    flex: 1 1 auto;
    text-align: center;
  }

  .bento-mini-row {
    gap: 8px;
  }

  .bento-mini-card {
    padding: 8px 10px;
  }

  .bento-title {
    font-size: 0.78rem;
  }

  .bento-desc {
    font-size: 0.68rem;
  }

  .precall-split {
    grid-template-columns: 1fr;
    gap: 16px;
  }

  .mirror-viewfinder-box {
    min-height: 280px;
  }

  /* Fullscreen Viewport Video Call Stage */
  .call-view {
    position: fixed;
    inset: 0;
    width: 100vw !important;
    height: 100vh !important;
    height: 100dvh !important;
    max-width: 100% !important;
    margin: 0 !important;
    padding: 0 !important;
    z-index: 100;
  }

  .video-stage-board {
    position: fixed;
    inset: 0;
    width: 100vw;
    height: 100vh;
    height: 100dvh;
    overflow: hidden;
  }

  .remote-video-canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    padding: 0;
    overflow: hidden;
  }

  .video-stream-wrap {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    min-height: 100%;
  }

  #remoteVideo {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 0;
  }

  /* Top HUD on Mobile */
  .camcorder-top-hud {
    position: absolute;
    top: max(14px, env(safe-area-inset-top));
    left: 12px;
    right: 12px;
    z-index: 40;
    width: auto;
  }

  .rec-live-tag {
    padding: 4px 8px;
    font-size: 0.68rem;
  }

  .room-id-tag {
    padding: 4px 8px;
    font-size: 0.68rem;
  }

  .telemetry-pill {
    display: none;
  }

  /* Floating Self PiP Viewfinder on Mobile */
  .floating-pip-card {
    position: absolute;
    top: max(56px, env(safe-area-inset-top));
    right: 14px;
    width: 100px;
    height: 145px;
    z-index: 50;
    border-radius: 8px;
    box-shadow: 4px 4px 0 var(--ink-black);
  }

  .pip-viewport {
    width: 100%;
    height: 100%;
  }

  /* Floating Bottom Controls Dock */
  .bottom-stage-controls {
    position: absolute;
    bottom: max(14px, env(safe-area-inset-bottom));
    left: 10px;
    right: 10px;
    z-index: 60;
    width: auto;
  }

  .tactile-control-dock {
    width: 100%;
    max-width: 360px;
    margin: 0 auto;
    gap: 4px;
    padding: 6px;
    flex-wrap: nowrap;
    justify-content: space-between;
    border-radius: 8px;
    box-shadow: 4px 4px 0 var(--ink-black);
  }

  .dock-btn {
    flex: 1 1 auto;
    padding: 10px 4px;
    font-size: 0.7rem;
    justify-content: center;
    gap: 0;
    min-width: 36px;
  }

  .dock-btn .dock-label {
    display: none;
  }

  .btn-dock-hangup {
    flex: 1.4 1 auto;
    padding: 10px 8px;
  }

  .btn-dock-hangup .dock-label {
    display: inline;
    font-size: 0.7rem;
    margin-left: 4px;
  }

  /* Slide-Up Bottom Sheet Drawer on Mobile */
  .telemetry-drawer-panel {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    width: 100%;
    max-height: 70vh;
    max-height: 70dvh;
    z-index: 200;
    border-top: var(--border-thick);
    border-left: none;
    border-radius: 16px 16px 0 0;
    box-shadow: 0 -10px 40px rgba(0,0,0,0.7);
    background: var(--bg-cream);
  }

  .telemetry-drawer-panel.collapsed {
    display: none !important;
  }
}

/* ========================================== */
/* PUSH NOTIFICATIONS & CONTACTS SECTION      */
/* ========================================== */

.push-contacts-grid {
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: 24px;
  width: 100%;
  margin-top: 24px;
}

@media (max-width: 900px) {
  .push-contacts-grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }
}

.push-card {
  background: var(--bg-paper);
}

.contacts-card {
  background: var(--pure-white);
}

.push-status-pill {
  font-size: 0.7rem;
  font-weight: 800;
  letter-spacing: 0.04em;
  padding: 3px 8px;
  border-radius: 4px;
  border: var(--border-thin);
}

.push-status-pill.pill-active {
  background: #10B981;
  color: #FFFFFF;
}

.push-status-pill.pill-inactive {
  background: #64748B;
  color: #FFFFFF;
}

.push-status-pill.pill-error {
  background: var(--hot-pink);
  color: #FFFFFF;
}

.push-setup-row {
  margin-top: 4px;
}

.btn-push-enable {
  width: 100%;
  height: 48px;
  background: var(--bg-cobalt);
  color: var(--pure-white);
  font-size: 0.88rem;
}

.btn-push-enable:hover {
  background: var(--hot-orange);
}

.my-card-content {
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin-top: 4px;
}

.input-label-row {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.field-label {
  font-size: 0.72rem;
  font-weight: 800;
  letter-spacing: 0.04em;
  color: var(--ink-black);
}

.card-copy-row {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 8px;
  background: var(--pure-white);
  border: var(--border-med);
  border-radius: 6px;
  padding: 4px 6px;
}

.card-copy-row input {
  width: 100%;
  border: none;
  outline: none;
  font-size: 0.78rem;
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-weight: 700;
  color: var(--ink-black);
  background: transparent;
  padding-left: 6px;
}

.card-info-note {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  font-size: 0.75rem;
  font-weight: 700;
  color: #475569;
  line-height: 1.35;
  background: var(--bg-cream);
  border: var(--border-thin);
  border-radius: 4px;
  padding: 8px 10px;
}

.card-info-note.ios-note {
  background: #FEF3C7;
  border-color: #F59E0B;
  color: #92400E;
  margin-top: 8px;
}

.card-info-note.ios-note .icon-inline {
  color: #D97706;
}

.card-info-note .icon-inline {
  color: var(--bg-cobalt);
  margin-top: 2px;
}

.add-contact-form {
  display: flex;
  flex-direction: column;
  gap: 10px;
  background: var(--bg-cream);
  border: var(--border-med);
  border-radius: 6px;
  padding: 12px;
}

.add-contact-inputs {
  display: grid;
  grid-template-columns: 1fr 1.5fr;
  gap: 8px;
}

@media (max-width: 600px) {
  .add-contact-inputs {
    grid-template-columns: 1fr;
  }
}

.btn-add-contact {
  height: 42px;
  background: var(--ink-black);
  color: var(--neon-lime);
  font-size: 0.84rem;
  width: 100%;
}

.btn-add-contact:hover {
  background: var(--bg-cobalt);
  color: var(--pure-white);
}

.contacts-list-container {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-top: 4px;
}

.contacts-list-header {
  font-size: 0.72rem;
  font-weight: 900;
  letter-spacing: 0.05em;
  color: #64748B;
  border-bottom: 2px solid #E2E8F0;
  padding-bottom: 4px;
}

.contacts-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
  max-height: 280px;
  overflow-y: auto;
  padding-right: 2px;
}

.contact-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-paper);
  border: var(--border-med);
  border-radius: 6px;
  padding: 10px 12px;
  gap: 10px;
  transition: transform 0.1s ease;
}

.contact-item:hover {
  transform: translateX(2px);
}

.contact-info {
  display: flex;
  align-items: center;
  gap: 10px;
  min-width: 0;
  flex: 1;
}

.contact-avatar {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: var(--neon-yellow);
  border: var(--border-med);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.95rem;
  font-weight: 900;
  color: var(--ink-black);
  flex-shrink: 0;
}

.contact-details {
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.contact-name {
  font-size: 0.88rem;
  font-weight: 800;
  color: var(--ink-black);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.contact-meta {
  font-size: 0.68rem;
  font-weight: 700;
  color: #059669;
}

.contact-actions {
  display: flex;
  align-items: center;
  gap: 6px;
  flex-shrink: 0;
}

.btn-call-contact {
  background: var(--bg-cobalt);
  color: var(--pure-white);
  padding: 7px 12px;
  font-size: 0.76rem;
  border-radius: 4px;
  border: var(--border-thin);
  font-weight: 800;
  display: inline-flex;
  align-items: center;
  gap: 6px;
}

.btn-call-contact:hover {
  background: #059669;
}

.btn-del-contact {
  background: transparent;
  color: #94A3B8;
  padding: 7px 8px;
  font-size: 0.72rem;
  border-radius: 4px;
  border: var(--border-thin);
  font-weight: 800;
}

.btn-del-contact:hover {
  background: var(--hot-pink);
  color: #FFFFFF;
  border-color: var(--ink-black);
}

.contacts-empty-state {
  font-size: 0.8rem;
  font-weight: 700;
  color: #64748B;
  padding: 20px 8px;
  text-align: center;
  line-height: 1.4;
  background: var(--bg-paper);
  border: 1.5px dashed #CBD5E1;
  border-radius: 6px;
}


</style>
</head>
<body>
  <main class="app-shell">
    
    <!-- ========================================== -->
    <!-- SCREEN 1: LOBBY / ROOM CREATOR             -->
    <!-- ========================================== -->
    <section id="homeView" class="board-card lobby-view">
      <!-- Nav Bar -->
      <header class="brutalist-nav">
        <div class="brand-mark">
          <img src="/assets/favicon.ico" alt="" class="brand-logo-img">
          <span class="brand-logo">GOCORD // P2P</span>
          <span class="live-dot" aria-hidden="true"></span>
        </div>
        <nav class="nav-links">
          <button id="navFriendsBtn" class="nav-link-btn" type="button">✦ CONTACTS</button>
          <span class="nav-link">SELF-HOST</span>
          <span class="nav-link">ROOMS</span>
          <span class="nav-link">DOCKER &amp; CADDY</span>
          <span class="nav-link">TELEMETRY</span>
        </nav>
        <div class="nav-header-actions">
          <button id="mobileNavFriendsBtn" class="nav-friends-mobile-btn" type="button">
            <span>✦ CONTACTS</span>
          </button>
          <button id="quickStartBtn" class="nav-cta-btn">
            <svg class="icon-inline" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 2v20M2 12h20"/></svg>
            <span>START CALL</span>
          </button>
        </div>
      </header>

      <!-- Ticker Marquee -->
      <div class="ticker-tape">
        <svg class="icon-inline" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
        <span class="ticker-text">100% P2P • ZERO SERVER LOGS • 256-BIT E2EE • DOCKER READY</span>
      </div>

      <!-- Main Hero Content -->
      <div class="hero-split">
        <!-- Left Hero Column -->
        <div class="hero-left">
          <div class="headline-block">
            <h1 class="hero-title-top">PRIVATE</h1>
            <div class="hero-title-bottom-row">
              <span class="hero-title-bottom">VIDEO CALLS</span>
              <span class="star-sticker">★ 60 FPS</span>
            </div>
          </div>
          <p class="hero-subhead">STANDALONE GO BINARY • DIRECT P2P MEDIA MESH • 256-BIT E2EE • H.264 HD</p>

          <!-- Primary Action Hub (Create Call & Call Friends) -->
          <div class="lobby-action-hub">
            <div class="lobby-cta-row">
              <!-- Action 1: Create Instant Room -->
              <button id="createButton" class="btn-brutalist btn-create-room">
                <svg class="icon-btn" viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2" ry="2"/></svg>
                <div class="btn-text-col">
                  <span class="btn-primary-title">CREATE ROOM</span>
                  <span class="btn-sub-tag">1-CLICK INSTANT ➜</span>
                </div>
              </button>

              <!-- Action 2: Call Friends -->
              <button id="openFriendsBtn" class="btn-brutalist btn-friends-card" type="button" title="Open Contact Book & Push Notifications">
                <svg class="icon-btn" viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
                <div class="btn-text-col">
                  <span class="btn-primary-title">CALL FRIENDS</span>
                  <span class="btn-sub-tag">CONTACT BOOK ⚡</span>
                </div>
              </button>
            </div>

            <!-- Action 3: Join Existing Room Input -->
            <div class="mobile-or-divider">
              <span>— OR JOIN EXISTING ROOM —</span>
            </div>

            <div class="action-card join-card-compact">
              <div class="mobile-action-header">
                <span class="action-tag">✦ ENTER INVITE LINK</span>
              </div>
              <div class="join-input-row">
                <div class="input-container">
                  <svg class="icon-input" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M9 17H7A5 5 0 0 1 7 7h2"/><path d="M15 7h2a5 5 0 1 1 0 10h-2"/><line x1="8" y1="12" x2="16" y2="12"/></svg>
                  <input id="joinInput" type="text" placeholder="Paste invite link or room code..." autocomplete="off" spellcheck="false">
                </div>
                <button id="joinButton" class="btn-brutalist btn-join">
                  <svg class="icon-btn" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
                  <span>JOIN ➜</span>
                </button>
              </div>
            </div>
          </div>

          <p id="homeError" class="error-banner" hidden></p>
        </div>

        <!-- Right Graphic Column -->
        <div class="hero-right">
          <div class="hud-mini-header">
            <span class="rec-mini-pill"><span class="rec-mini-dot"></span> LIVE ● P2P</span>
            <span class="hud-stats-text">1080p // 60fps // 48kHz</span>
          </div>

          <div class="hero-logo-box">
            <img src="/assets/bg-removed-logo.png" alt="Gocord Logo" class="hero-logo-img">
          </div>

          <div class="stickers-row">
            <span class="sticker-chip chip-lime">★ DOCKER READY</span>
            <span class="sticker-chip chip-orange">GOLANG WEBRTC ⚡</span>
          </div>

          <div class="bento-mini-row">
            <div class="bento-mini-card bento-zerologs">
              <span class="bento-title">ZERO //</span>
              <span class="bento-desc">Server Logs</span>
            </div>
            <div class="bento-mini-card bento-coturn">
              <span class="bento-title">256-BIT //</span>
              <span class="bento-desc">E2E Encrypted</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ========================================== -->
    <!-- SCREEN: FRIENDS / CONTACT BOOK             -->
    <!-- ========================================== -->
    <section id="friendsView" class="board-card friends-view" hidden>
      <header class="precall-header">
        <div class="brand-mark">
          <img src="/assets/favicon.ico" alt="" class="brand-logo-img">
          <span class="brand-logo">GOCORD // CONTACT BOOK</span>
        </div>
        <div class="precall-header-actions">
          <button id="backToHomeFromFriendsBtn" class="btn-brutalist btn-nav-new-room" type="button" title="Back to Lobby">
            <svg class="icon-inline" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>
            <span>BACK TO LOBBY</span>
          </button>
        </div>
      </header>

      <div class="push-contacts-grid">
        <!-- Card 1: My Call Card & Push Setup -->
        <div class="action-card push-card">
          <div class="action-card-header">
            <span class="action-tag">✦ MY PUSH CALL CARD</span>
            <span id="pushStatusBadge" class="push-status-pill pill-inactive">● PUSH INACTIVE</span>
          </div>
          
          <p class="action-desc">
            Enable browser push notifications to receive incoming calls without any central account or server database.
          </p>

          <div id="pushSetupBlock" class="push-setup-row">
            <button id="enablePushBtn" class="btn-brutalist btn-push-enable">
              <svg class="icon-btn" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>
              <span id="enablePushBtnText">ENABLE CALL NOTIFICATIONS</span>
            </button>
          </div>

          <div id="myCardBlock" class="my-card-content" hidden>
            <div class="input-label-row">
              <label for="myDisplayName" class="field-label">YOUR DISPLAY NAME (LOCAL ONLY):</label>
              <div class="input-container">
                <svg class="icon-input" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
                <input id="myDisplayName" type="text" placeholder="e.g. Alice" maxlength="32">
              </div>
            </div>

            <div class="copy-input-row card-copy-row">
              <input id="myCallCardLink" readonly spellcheck="false" placeholder="Generating Call Card link...">
              <button id="copyCallCardBtn" class="btn-brutalist btn-copy">
                <svg class="icon-inline" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
                <span>COPY LINK</span>
              </button>
            </div>
            
            <div class="card-info-note">
              <svg class="icon-inline" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg>
              <span>Share this Call Card link with friends. They can add you with 1 click to ring your browser directly.</span>
            </div>
          </div>

          <div id="iosPushBanner" class="card-info-note ios-note" hidden>
            <svg class="icon-inline" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="5" y="2" width="14" height="20" rx="2" ry="2"/><line x1="12" y1="18" x2="12.01" y2="18"/></svg>
            <span><strong>iOS (iPhone/iPad) Note:</strong> Apple requires web apps to be installed to the Home Screen for push notifications. Tap <strong>Share (⎋)</strong> ➔ <strong>"Add to Home Screen"</strong>, then open from your home screen.</span>
          </div>

          <p id="pushError" class="error-banner" hidden></p>
        </div>

        <!-- Card 2: Contact Book (100% Local / Zero Server Storage) -->
        <div class="action-card contacts-card">
          <div class="action-card-header">
            <span class="action-tag">✦ LOCAL CONTACT BOOK</span>
            <span class="badge-pill">100% PRIVATE</span>
          </div>

          <!-- Add Contact Form -->
          <div class="add-contact-form">
            <div class="add-contact-inputs">
              <div class="input-container contact-name-input-box">
                <svg class="icon-input" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
                <input id="contactNameInput" type="text" placeholder="Contact name (e.g. Bob)" autocomplete="off">
              </div>
              <div class="input-container contact-card-input-box">
                <svg class="icon-input" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>
                <input id="contactCardInput" type="text" placeholder="Paste Call Card link (#card=...) or code" autocomplete="off">
              </div>
            </div>
            <button id="saveContactBtn" class="btn-brutalist btn-add-contact">
              <svg class="icon-btn" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
              <span>SAVE CONTACT</span>
            </button>
          </div>

          <p id="contactError" class="error-banner" hidden></p>
          <p id="contactSuccess" class="status-banner" hidden></p>

          <!-- Contacts List -->
          <div class="contacts-list-container">
            <div class="contacts-list-header">
              <span>SAVED CONTACTS (<span id="contactsCount">0</span>)</span>
            </div>
            <div id="contactsList" class="contacts-list">
              <!-- Rendered dynamically by JavaScript -->
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ========================================== -->
    <!-- SCREEN 2: PRE-CALL / GREEN ROOM VIEW       -->
    <!-- ========================================== -->
    <section id="preCallView" class="board-card precall-view" hidden>
      <!-- Precall Header -->
      <header class="precall-header">
        <div class="brand-mark">
          <a href="/" class="brand-logo">GOCORD // GREEN ROOM</a>
        </div>
        <div class="precall-header-actions">
          <button id="precallCreateRoomNavBtn" class="btn-brutalist btn-nav-new-room" title="Create a new instant room">
            <svg class="icon-inline" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 2v20M2 12h20"/></svg>
            <span>NEW ROOM</span>
          </button>
          <div class="precall-room-tag">
            <span id="preCallRoomBadge">✦ GOCORD READY: #ROOM</span>
          </div>
        </div>
      </header>

      <div class="precall-split">
        <!-- Left: Camera Preview Viewfinder -->
        <div class="mirror-viewfinder-box">
          <div class="viewfinder-top-hud">
            <span class="viewfinder-tag">FACETIME HD 1080P</span>
            <div class="vu-meter-pill">
              <svg class="icon-inline" viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/><line x1="12" y1="19" x2="12" y2="23"/><line x1="8" y1="23" x2="16" y2="23"/></svg>
              <div id="precallVuMeter" class="vu-bars" aria-label="Microphone VU meter">
                <span class="bar"></span><span class="bar"></span><span class="bar"></span><span class="bar"></span>
                <span class="bar"></span><span class="bar"></span><span class="bar"></span><span class="bar"></span>
              </div>
            </div>
          </div>

          <div class="mirror-video-container">
            <video id="previewVideo" autoplay playsinline muted></video>
            <div id="previewFallback" class="mirror-fallback" hidden>
              <div class="avatar-disc-large">
                <svg viewBox="0 0 24 24" width="40" height="40" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
              </div>
              <span class="mirror-fallback-text">CAMERA OFF</span>
            </div>
          </div>

          <div class="viewfinder-bottom-toggles">
            <button id="precallMicToggle" class="btn-toggle toggle-mic active" aria-pressed="true">
              <svg class="icon-btn" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/></svg>
              <span id="precallMicLabel">MIC ACTIVE</span>
            </button>
            <button id="precallCamToggle" class="btn-toggle toggle-cam active" aria-pressed="true">
              <svg class="icon-btn" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2" ry="2"/></svg>
              <span id="precallCamLabel">CAM ACTIVE</span>
            </button>
            <button id="precallNoiseToggle" class="btn-toggle toggle-noise active" aria-pressed="true">
              <svg class="icon-btn" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><polyline points="9 12 11 14 15 10"/></svg>
              <span>NOISE SUPPRESSION</span>
            </button>
          </div>
        </div>

        <!-- Right: Invite Sheet & Hardware Pickers -->
        <div class="precall-actions-column">
          <!-- Invite Share Card -->
          <div id="inviteBlock" class="invite-share-card">
            <span class="invite-card-title">SHARE CALL INVITATION ✦</span>
            <p class="invite-card-sub">The secret after # is end-to-end and never touches the server.</p>
            <div class="copy-input-row">
              <input id="inviteURL" readonly spellcheck="false">
              <button id="copyButton" class="btn-brutalist btn-copy">
                <svg class="icon-inline" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
                <span>COPY</span>
              </button>
            </div>
          </div>

          <!-- Hardware Device Selectors -->
          <div class="device-pickers-card">
            <span class="pickers-title">SELECT HARDWARE DEVICES</span>
            
            <div class="device-row">
              <label for="audioSource" class="device-label">
                <svg class="icon-inline" viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/></svg>
                <span>MICROPHONE</span>
              </label>
              <select id="audioSource" class="device-select"></select>
            </div>

            <div class="device-row">
              <label for="audioOutput" class="device-label">
                <svg class="icon-inline" viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14"/><path d="M15.54 8.46a5 5 0 0 1 0 7.07"/></svg>
                <span>OUTPUT SPEAKER</span>
              </label>
              <select id="audioOutput" class="device-select"></select>
            </div>

            <div class="device-row">
              <label for="videoSource" class="device-label">
                <svg class="icon-inline" viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2" ry="2"/></svg>
                <span>CAMERA LENS</span>
              </label>
              <select id="videoSource" class="device-select"></select>
            </div>
          </div>

          <p id="preCallStatus" class="status-banner">Checking room status…</p>
          <p id="preCallError" class="error-banner" hidden></p>

          <!-- Giant Join Button -->
          <button id="startButton" class="btn-brutalist btn-giant-join">
            <svg class="icon-btn" viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
            <span>JOIN VIDEO ROOM NOW ➜</span>
          </button>

          <!-- Secondary Action: Create Another Room -->
          <button id="precallCreateNewBtn" class="btn-brutalist btn-create-another">
            <svg class="icon-btn" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2" ry="2"/></svg>
            <span>CREATE ANOTHER ROOM ➜</span>
          </button>
        </div>
      </div>
    </section>

    <!-- ========================================== -->
    <!-- SCREEN 3: ACTIVE CALL & BROADCAST STAGE    -->
    <!-- ========================================== -->
    <section id="callView" class="board-card call-view" hidden>
      <div class="video-stage-board">
        <!-- Main Remote Video Viewport -->
        <div class="remote-video-canvas">
          
          <!-- Top Broadcast Camcorder HUD -->
          <div class="camcorder-top-hud">
            <div class="hud-group-left">
              <span class="rec-live-tag">
                <span class="rec-dot-pulsing"></span>
                <span id="callDurationText">LIVE [00:00]</span>
              </span>
              <button id="callRoomBadge" class="room-id-tag" title="Click to copy invite link">
                <span id="callRoomName">ROOM #8492</span>
                <svg class="icon-inline" viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
              </button>
            </div>

            <div class="hud-group-right">
              <span id="callStatsBadge" class="telemetry-pill">1080P 60FPS // 14ms RTT // E2EE</span>
            </div>
          </div>

          <!-- Remote Video Feed -->
          <div class="video-stream-wrap">
            <video id="remoteVideo" autoplay playsinline></video>
            
            <div id="remotePlaceholder" class="remote-placeholder-box">
              <div class="remote-avatar-disc">
                <svg viewBox="0 0 24 24" width="48" height="48" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
              </div>
              <span id="remotePeerName" class="remote-peer-title">WAITING FOR PEER TO JOIN…</span>
              <div class="speaking-status-pill">
                <svg class="icon-inline" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M2 10v3"/><path d="M6 6v11"/><path d="M10 3v18"/><path d="M14 8v7"/><path d="M18 5v13"/><path d="M22 10v3"/></svg>
                <span id="connectionStatus">WAITING FOR PEER…</span>
              </div>
            </div>

            <button id="playRemoteButton" class="btn-play-remote" hidden>Tap to enable remote audio</button>

            <!-- Floating Self-Camera PiP Viewfinder (Draggable & Snappable) -->
            <div id="selfPipCard" class="floating-pip-card" title="Drag to reposition">
              <div class="pip-viewport">
                <video id="localVideo" autoplay playsinline muted></video>
              </div>
            </div>
          </div>

          <!-- Bottom Stage Controls Stack -->
          <div class="bottom-stage-controls">
            <!-- Floating Tactile Control Dock -->
            <div class="tactile-control-dock" role="toolbar" aria-label="Call controls">
              <button id="muteButton" class="dock-btn btn-dock-mic" aria-pressed="false" title="Mute/Unmute microphone">
                <svg class="icon-dock" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/><line x1="12" y1="19" x2="12" y2="23"/><line x1="8" y1="23" x2="16" y2="23"/></svg>
                <span class="dock-label">MIC ON</span>
              </button>

              <button id="cameraButton" class="dock-btn btn-dock-cam" aria-pressed="false" title="Enable/Disable camera">
                <svg class="icon-dock" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2" ry="2"/></svg>
                <span class="dock-label">CAM ON</span>
              </button>

              <button id="shareScreenButton" class="dock-btn btn-dock-share" aria-pressed="false" title="Share Screen">
                <svg class="icon-dock" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
                <span class="dock-label">SHARE</span>
              </button>

              <button id="voiceFxButton" class="dock-btn btn-dock-fx" aria-pressed="false" title="Voice Noise Filter">
                <svg class="icon-dock" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 2v20M2 12h20M4.93 4.93l14.14 14.14M4.93 19.07l14.14-14.14"/></svg>
                <span class="dock-label">VOICE FX</span>
              </button>

              <button id="toggleDrawerButton" class="dock-btn btn-dock-settings" aria-expanded="true" title="Toggle Settings & Stats">
                <svg class="icon-dock" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="4" y1="21" x2="4" y2="14"/><line x1="4" y1="10" x2="4" y2="3"/><line x1="12" y1="21" x2="12" y2="12"/><line x1="12" y1="8" x2="12" y2="3"/><line x1="20" y1="21" x2="20" y2="16"/><line x1="20" y1="12" x2="20" y2="3"/><line x1="1" y1="14" x2="7" y2="14"/><line x1="9" y1="8" x2="15" y2="8"/><line x1="17" y1="16" x2="23" y2="16"/></svg>
                <span class="dock-label">SETTINGS &amp; STATS</span>
              </button>

              <button id="hangupButton" class="dock-btn btn-dock-hangup" title="Hang up and leave call">
                <svg class="icon-dock" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M10.68 13.31a16 16 0 0 0 3.41 2.6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7 2 2 0 0 1 1.72 2v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.42 19.42 0 0 1-3.33-2.67m-2.67-3.34a19.79 19.79 0 0 1-3.07-8.63A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91"/><line x1="1" y1="1" x2="23" y2="23"/></svg>
                <span class="dock-label">END CALL</span>
              </button>
            </div>
          </div>
        </div>

        <!-- Collapsible Media & Telemetry Drawer -->
        <aside id="telemetryDrawer" class="telemetry-drawer-panel">
          <div class="drawer-header">
            <span class="drawer-title">MEDIA &amp; TELEMETRY</span>
            <button id="closeDrawerButton" class="btn-drawer-collapse">✕ HIDE PANEL</button>
          </div>

          <!-- In-call Device Pickers -->
          <div class="drawer-section">
            <span class="drawer-section-title">INPUT / OUTPUT DEVICES</span>
            <div class="drawer-input-group">
              <label for="drawerAudioSource" class="drawer-field-label">
                <svg class="icon-inline" viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/></svg>
                <span>MICROPHONE</span>
              </label>
              <select id="drawerAudioSource" class="drawer-select"></select>
            </div>
            <div class="drawer-input-group">
              <label for="drawerAudioOutput" class="drawer-field-label">
                <svg class="icon-inline" viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14"/></svg>
                <span>SPEAKER</span>
              </label>
              <select id="drawerAudioOutput" class="drawer-select"></select>
            </div>
            <div class="drawer-input-group">
              <label for="drawerVideoSource" class="drawer-field-label">
                <svg class="icon-inline" viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2" ry="2"/></svg>
                <span>CAMERA</span>
              </label>
              <select id="drawerVideoSource" class="drawer-select"></select>
            </div>
          </div>

          <!-- Video Stream Profile Switcher -->
          <div class="drawer-section">
            <span class="drawer-section-title">VIDEO STREAM PROFILE</span>
            <div class="quality-pills-row">
              <button class="btn-quality-pill" data-quality="quality">HD 60FPS</button>
              <button class="btn-quality-pill active" data-quality="auto">AUTO</button>
              <button class="btn-quality-pill" data-quality="low">LOW B/W</button>
            </div>
            <select id="qualitySelect" hidden>
              <option value="auto" selected>Auto</option>
              <option value="quality">Quality</option>
              <option value="low">Low bandwidth</option>
            </select>
          </div>

          <!-- Video Fit Switcher -->
          <div class="drawer-section">
            <span class="drawer-section-title">VIDEO FIT MODE</span>
            <div class="quality-pills-row">
              <button class="btn-fit-pill btn-quality-pill active" data-fit="contain">CONTAIN</button>
              <button class="btn-fit-pill btn-quality-pill" data-fit="cover">COVER</button>
            </div>
          </div>

          <!-- WebRTC Live Telemetry Matrix -->
          <div class="drawer-section">
            <span class="drawer-section-title">LIVE WEBRTC METRICS</span>
            <div class="metrics-grid">
              <div class="metric-tile">
                <span class="metric-label">BITRATE</span>
                <span id="metricBitrate" class="metric-val val-blue">-- Mbps</span>
              </div>
              <div class="metric-tile">
                <span class="metric-label">RTT LATENCY</span>
                <span id="metricRtt" class="metric-val val-pink">-- ms</span>
              </div>
              <div class="metric-tile">
                <span class="metric-label">PACKET LOSS</span>
                <span id="metricLoss" class="metric-val val-dark">0.0%</span>
              </div>
              <div class="metric-tile">
                <span class="metric-label">ICE CANDIDATE</span>
                <span id="metricCandidate" class="metric-val val-orange">P2P Host</span>
              </div>
              <div class="metric-tile">
                <span class="metric-label">CODEC</span>
                <span id="metricCodec" class="metric-val val-dark">--</span>
              </div>
              <div class="metric-tile">
                <span class="metric-label">RESOLUTION</span>
                <span id="metricResolution" class="metric-val val-blue">--</span>
              </div>
            </div>
            <dl id="diagnosticsGrid" class="raw-diagnostics-table" hidden></dl>
          </div>
        </aside>
      </div>
    </section>

  </main>

  <script>
(() => {
  "use strict";

  const els = {
    // Screen 1: Lobby
    homeView: document.getElementById("homeView"),
    createButton: document.getElementById("createButton"),
    quickStartBtn: document.getElementById("quickStartBtn"),
    joinInput: document.getElementById("joinInput"),
    joinButton: document.getElementById("joinButton"),
    homeError: document.getElementById("homeError"),
    openFriendsBtn: document.getElementById("openFriendsBtn"),
    navFriendsBtn: document.getElementById("navFriendsBtn"),
    mobileNavFriendsBtn: document.getElementById("mobileNavFriendsBtn"),

    // Screen: Friends / Contact Book
    friendsView: document.getElementById("friendsView"),
    backToHomeFromFriendsBtn: document.getElementById("backToHomeFromFriendsBtn"),
    pushStatusBadge: document.getElementById("pushStatusBadge"),
    enablePushBtn: document.getElementById("enablePushBtn"),
    enablePushBtnText: document.getElementById("enablePushBtnText"),
    pushSetupBlock: document.getElementById("pushSetupBlock"),
    myCardBlock: document.getElementById("myCardBlock"),
    myDisplayName: document.getElementById("myDisplayName"),
    myCallCardLink: document.getElementById("myCallCardLink"),
    copyCallCardBtn: document.getElementById("copyCallCardBtn"),
    iosPushBanner: document.getElementById("iosPushBanner"),
    pushError: document.getElementById("pushError"),
    contactNameInput: document.getElementById("contactNameInput"),
    contactCardInput: document.getElementById("contactCardInput"),
    saveContactBtn: document.getElementById("saveContactBtn"),
    contactError: document.getElementById("contactError"),
    contactSuccess: document.getElementById("contactSuccess"),
    contactsCount: document.getElementById("contactsCount"),
    contactsList: document.getElementById("contactsList"),

    // Screen 2: Pre-Call / Green Room
    preCallView: document.getElementById("preCallView"),
    preCallRoomBadge: document.getElementById("preCallRoomBadge"),
    precallCreateRoomNavBtn: document.getElementById("precallCreateRoomNavBtn"),
    inviteBlock: document.getElementById("inviteBlock"),
    inviteURL: document.getElementById("inviteURL"),
    copyButton: document.getElementById("copyButton"),
    previewVideo: document.getElementById("previewVideo"),
    previewFallback: document.getElementById("previewFallback"),
    precallVuMeter: document.getElementById("precallVuMeter"),
    precallMicToggle: document.getElementById("precallMicToggle"),
    precallCamToggle: document.getElementById("precallCamToggle"),
    precallNoiseToggle: document.getElementById("precallNoiseToggle"),
    precallMicLabel: document.getElementById("precallMicLabel"),
    precallCamLabel: document.getElementById("precallCamLabel"),
    audioSource: document.getElementById("audioSource"),
    audioOutput: document.getElementById("audioOutput"),
    videoSource: document.getElementById("videoSource"),
    preCallStatus: document.getElementById("preCallStatus"),
    preCallError: document.getElementById("preCallError"),
    startButton: document.getElementById("startButton"),
    precallCreateNewBtn: document.getElementById("precallCreateNewBtn"),

    // Screen 3: Active Call
    callView: document.getElementById("callView"),
    callDurationText: document.getElementById("callDurationText"),
    callRoomBadge: document.getElementById("callRoomBadge"),
    callRoomName: document.getElementById("callRoomName"),
    callStatsBadge: document.getElementById("callStatsBadge"),
    remoteVideo: document.getElementById("remoteVideo"),
    remotePlaceholder: document.getElementById("remotePlaceholder"),
    remotePeerName: document.getElementById("remotePeerName"),
    connectionStatus: document.getElementById("connectionStatus"),
    playRemoteButton: document.getElementById("playRemoteButton"),
    selfPipCard: document.getElementById("selfPipCard"),
    localVideo: document.getElementById("localVideo"),
    switchCameraButton: document.getElementById("switchCameraButton"),
    pipVuMeter: document.getElementById("pipVuMeter"),

    // Bottom Controls Dock
    muteButton: document.getElementById("muteButton"),
    cameraButton: document.getElementById("cameraButton"),
    shareScreenButton: document.getElementById("shareScreenButton"),
    voiceFxButton: document.getElementById("voiceFxButton"),
    toggleDrawerButton: document.getElementById("toggleDrawerButton"),
    hangupButton: document.getElementById("hangupButton"),

    // Collapsible Drawer & Telemetry
    telemetryDrawer: document.getElementById("telemetryDrawer"),
    closeDrawerButton: document.getElementById("closeDrawerButton"),
    drawerAudioSource: document.getElementById("drawerAudioSource"),
    drawerAudioOutput: document.getElementById("drawerAudioOutput"),
    drawerVideoSource: document.getElementById("drawerVideoSource"),
    qualitySelect: document.getElementById("qualitySelect"),
    metricBitrate: document.getElementById("metricBitrate"),
    metricRtt: document.getElementById("metricRtt"),
    metricLoss: document.getElementById("metricLoss"),
    metricCandidate: document.getElementById("metricCandidate"),
    metricCodec: document.getElementById("metricCodec"),
    metricResolution: document.getElementById("metricResolution"),
    diagnosticsGrid: document.getElementById("diagnosticsGrid"),
  };

  const state = {
    room: "",
    secret: "",
    inviteURL: "",
    clientID: loadClientID(),
    role: "",
    ws: null,
    pc: null,
    localStream: null,
    previewStream: null,
    screenStream: null,
    remoteStream: null,
    config: null,
    started: false,
    intentionalClose: false,
    makingOffer: false,
    pendingCandidates: [],
    reconnectTimer: null,
    disconnectTimer: null,
    peerGoneTimer: null,
    reconnectAttempts: 0,
    iceRestartInFlight: false,
    terminal: false,
    statsTimer: null,
    callDurationTimer: null,
    callStartTime: 0,
    facingMode: "user",
    lastBytesSent: 0,
    lastStatsAt: 0,
    selectedPairVerified: false,
    micEnabled: true,
    camEnabled: true,
    noiseSuppression: true,
    isSharingScreen: false,
    audioContext: null,
    analyser: null,
    animFrameId: null,
    pushSubscription: null,
    myDisplayName: localStorage.getItem("gocord.displayName") || "",
    contacts: [],
    swRegistration: null,
  };
  let audioCtx = null;
  function playSound(type) {
    if (!audioCtx) {
      audioCtx = new (window.AudioContext || window.webkitAudioContext)();
    }
    if (audioCtx.state === "suspended") audioCtx.resume();

    const osc = audioCtx.createOscillator();
    const gainNode = audioCtx.createGain();
    osc.connect(gainNode);
    gainNode.connect(audioCtx.destination);
    const now = audioCtx.currentTime;

    if (type === "unmute") {
      osc.type = "sine";
      osc.frequency.setValueAtTime(400, now);
      osc.frequency.exponentialRampToValueAtTime(800, now + 0.1);
      gainNode.gain.setValueAtTime(0, now);
      gainNode.gain.linearRampToValueAtTime(0.5, now + 0.02);
      gainNode.gain.exponentialRampToValueAtTime(0.01, now + 0.1);
      osc.start(now);
      osc.stop(now + 0.1);
    } else if (type === "mute") {
      osc.type = "sine";
      osc.frequency.setValueAtTime(400, now);
      osc.frequency.exponentialRampToValueAtTime(200, now + 0.15);
      gainNode.gain.setValueAtTime(0, now);
      gainNode.gain.linearRampToValueAtTime(0.5, now + 0.02);
      gainNode.gain.exponentialRampToValueAtTime(0.01, now + 0.15);
      osc.start(now);
      osc.stop(now + 0.15);
    } else if (type === "join") {
      const osc1 = audioCtx.createOscillator();
      const gain1 = audioCtx.createGain();
      osc1.connect(gain1);
      gain1.connect(audioCtx.destination);
      osc1.type = "sine";
      osc1.frequency.value = 523.25;
      gain1.gain.setValueAtTime(0, now);
      gain1.gain.linearRampToValueAtTime(0.3, now + 0.05);
      gain1.gain.exponentialRampToValueAtTime(0.01, now + 0.3);
      osc1.start(now);
      osc1.stop(now + 0.3);

      const osc2 = audioCtx.createOscillator();
      const gain2 = audioCtx.createGain();
      osc2.connect(gain2);
      gain2.connect(audioCtx.destination);
      osc2.type = "sine";
      osc2.frequency.value = 659.25;
      gain2.gain.setValueAtTime(0, now + 0.1);
      gain2.gain.linearRampToValueAtTime(0.3, now + 0.15);
      gain2.gain.exponentialRampToValueAtTime(0.01, now + 0.4);
      osc2.start(now + 0.1);
      osc2.stop(now + 0.4);
    } else if (type === "leave") {
      osc.type = "sine";
      osc.frequency.setValueAtTime(523.25, now);
      osc.frequency.exponentialRampToValueAtTime(261.63, now + 0.2);
      gainNode.gain.setValueAtTime(0, now);
      gainNode.gain.linearRampToValueAtTime(0.3, now + 0.05);
      gainNode.gain.exponentialRampToValueAtTime(0.01, now + 0.3);
      osc.start(now);
      osc.stop(now + 0.3);
    }
  }

  // Recovery tuning. The server holds a room for EMPTY_ROOM_GRACE (45s by
  // default) after the last peer drops, so the client's patience is matched to
  // roughly that window.
  const SIGNALING_RETRY_BASE_MS = 1000;
  const SIGNALING_RETRY_MAX_MS = 15000;
  const ICE_DISCONNECT_GRACE_MS = 2500; // "disconnected" frequently self-heals
  const PEER_RETURN_TIMEOUT_MS = 60000;

  // Join errors that no amount of retrying will fix.
  const TERMINAL_SIGNALING_CODES = new Set([
    "room_not_found", "room_expired", "unauthorized", "room_full", "at_capacity", "bad_join",
  ]);

  const videoConstraints = {
    width: { ideal: 1920 },
    height: { ideal: 1080 },
    frameRate: { ideal: 30, max: 60 },
  };

  function loadClientID() {
    const key = "gocord.clientID";
    let id = sessionStorage.getItem(key);
    if (!id) {
      id = crypto.randomUUID ? crypto.randomUUID() : ` + "`" + `${Date.now()}-${Math.random().toString(36).slice(2)}` + "`" + `;
      sessionStorage.setItem(key, id);
    }
    return id;
  }

  // ==========================================
  // VIDEO CODEC PREFERENCE
  // ==========================================

  // H.264 is pinned first: it is hardware encoded on effectively every device,
  // which gives the lowest encode latency and CPU cost of the available codecs.
  // The remaining codecs stay in the list behind it so a peer that cannot do
  // H.264 still negotiates something rather than failing outright.
  const PREFERRED_VIDEO_CODEC = "video/H264";

  // Reorders the video transceiver's codec list. Must run before createOffer,
  // since the answering side only honours what the offer already lists.
  function preferVideoCodec(pc) {
    if (!pc || !window.RTCRtpSender || !RTCRtpSender.getCapabilities) return;
    if (!window.RTCRtpTransceiver || !("setCodecPreferences" in RTCRtpTransceiver.prototype)) return;

    const transceiver = pc.getTransceivers()
      .find(t => t.sender?.track?.kind === "video" || t.receiver?.track?.kind === "video");
    if (!transceiver) return;

    const all = RTCRtpSender.getCapabilities("video")?.codecs || [];
    const want = PREFERRED_VIDEO_CODEC.toLowerCase();
    const preferred = all.filter(c => (c.mimeType || "").toLowerCase() === want);
    if (!preferred.length) return; // Not supported here; leave the default order.

    try {
      transceiver.setCodecPreferences([...preferred, ...all.filter(c => !preferred.includes(c))]);
    } catch (e) {
      console.warn("Could not apply codec preference:", e);
    }
  }

  // ==========================================
  // OPUS AUDIO TUNING FOR WEAK NETWORKS
  // ==========================================

  // In-band Forward Error Correction (FEC), Discontinuous Transmission (DTX),
  // mono voice, and 28 kbps bitrate constraint for packet-loss resiliency.
  function enhanceOpusSDP(sdp) {
    if (!sdp || typeof sdp !== "string") return sdp;
    const separator = sdp.includes("\r\n") ? "\r\n" : "\n";
    const lines = sdp.split(separator);
    let opusPt = null;

    for (const line of lines) {
      const match = line.match(/^a=rtpmap:(\d+)\s+opus\/48000/i);
      if (match) {
        opusPt = match[1];
        break;
      }
    }

    if (!opusPt) return sdp;

    const opusParams = "useinbandfec=1;usedtx=1;stereo=0;sprop-stereo=0;maxaveragebitrate=28000;cbr=0";
    let foundFmtp = false;
    const newLines = [];

    for (const line of lines) {
      if (line.startsWith(` + "`" + `a=fmtp:${opusPt} ` + "`" + `) || line.startsWith(` + "`" + `a=fmtp:${opusPt}=` + "`" + `)) {
        foundFmtp = true;
        const prefix = ` + "`" + `a=fmtp:${opusPt} ` + "`" + `;
        const existingVal = line.substring(prefix.length);
        const merged = mergeFmtpParams(existingVal, opusParams);
        newLines.push(` + "`" + `${prefix}${merged}` + "`" + `);
      } else {
        newLines.push(line);
      }
    }

    if (!foundFmtp) {
      const finalLines = [];
      for (const line of newLines) {
        finalLines.push(line);
        if (line.match(new RegExp(` + "`" + `^a=rtpmap:${opusPt}\\s+opus` + "`" + `, "i"))) {
          finalLines.push(` + "`" + `a=fmtp:${opusPt} ${opusParams}` + "`" + `);
        }
      }
      return finalLines.join(separator);
    }

    return newLines.join(separator);
  }

  function mergeFmtpParams(existing, desired) {
    const map = new Map();
    if (existing) {
      existing.split(";").forEach(pair => {
        const idx = pair.indexOf("=");
        if (idx > 0) {
          map.set(pair.substring(0, idx).trim().toLowerCase(), pair.substring(idx + 1).trim());
        } else if (pair.trim()) {
          map.set(pair.trim().toLowerCase(), "");
        }
      });
    }
    desired.split(";").forEach(pair => {
      const idx = pair.indexOf("=");
      if (idx > 0) {
        map.set(pair.substring(0, idx).trim().toLowerCase(), pair.substring(idx + 1).trim());
      } else if (pair.trim()) {
        map.set(pair.trim().toLowerCase(), "");
      }
    });
    const result = [];
    for (const [k, v] of map.entries()) {
      result.push(v !== "" ? ` + "`" + `${k}=${v}` + "`" + ` : k);
    }
    return result.join("; ");
  }

  // ==========================================
  // AUDIO-FIRST NETWORK PRIORITY ALLOCATION
  // ==========================================

  // Prioritizes audio packets over video in the WebRTC congestion controller (BBR/GCC)
  // and configures video degradation to drop resolution instead of stuttering framerate.
  function configureRtpPriorities(pc) {
    if (!pc || !pc.getSenders) return;
    try {
      for (const sender of pc.getSenders()) {
        if (!sender.track || !sender.getParameters || !sender.setParameters) continue;
        const params = sender.getParameters();
        if (!params.encodings || params.encodings.length === 0) {
          params.encodings = [{}];
        }

        if (sender.track.kind === "audio") {
          params.encodings[0].priority = "high";
          params.encodings[0].networkPriority = "high";
        } else if (sender.track.kind === "video") {
          params.encodings[0].priority = "low";
          params.encodings[0].networkPriority = "low";
          if ("degradationPreference" in params) {
            params.degradationPreference = "maintain-framerate";
          }
        }
        sender.setParameters(params).catch(() => {});
      }
    } catch (_) {}
  }

  function parseRoomFromLocation() {
    const match = location.pathname.match(/^\/join\/([A-Za-z0-9_-]+)$/);
    if (!match) return null;
    const room = match[1];
    let secret = location.hash ? location.hash.slice(1) : "";

    if (secret) {
      try {
        sessionStorage.setItem(` + "`" + `gocord.secret.${room}` + "`" + `, secret);
      } catch (_) {}
      // Occult the secret from the browser address bar immediately
      history.replaceState(null, "", ` + "`" + `/join/${encodeURIComponent(room)}` + "`" + `);
    } else {
      try {
        secret = sessionStorage.getItem(` + "`" + `gocord.secret.${room}` + "`" + `) || "";
      } catch (_) {}
    }

    return { room, secret };
  }

  function show(view) {
    els.homeView.hidden = view !== "home";
    if (els.friendsView) els.friendsView.hidden = view !== "friends";
    els.preCallView.hidden = view !== "precall";
    els.callView.hidden = view !== "call";
  }

  function setStatus(text) {
    if (els.connectionStatus) els.connectionStatus.textContent = text.toUpperCase();
    if (els.preCallStatus) els.preCallStatus.textContent = text;
  }

  function showError(element, message) {
    if (!element) return;
    element.textContent = message;
    element.hidden = !message;
  }

  async function api(path, options = {}) {
    const response = await fetch(path, {
      ...options,
      headers: { "Content-Type": "application/json", ...(options.headers || {}) },
      cache: "no-store",
    });
    let body = null;
    try { body = await response.json(); } catch (_) {}
    if (!response.ok) {
      throw new Error(body?.error || ` + "`" + `Request failed (${response.status})` + "`" + `);
    }
    return body;
  }

  // ==========================================
  // PUSH NOTIFICATIONS & LOCAL CONTACTS SYSTEM
  // ==========================================

  function urlBase64ToUint8Array(base64String) {
    const padding = "=".repeat((4 - (base64String.length % 4)) % 4);
    const base64 = (base64String + padding).replace(/-/g, "+").replace(/_/g, "/");
    const rawData = window.atob(base64);
    const outputArray = new Uint8Array(rawData.length);
    for (let i = 0; i < rawData.length; ++i) {
      outputArray[i] = rawData.charCodeAt(i);
    }
    return outputArray;
  }

  function encodeBase64URL(str) {
    return btoa(unescape(encodeURIComponent(str)))
      .replace(/\+/g, "-")
      .replace(/\//g, "_")
      .replace(/=+$/, "");
  }

  function decodeBase64URL(str) {
    let base64 = str.replace(/-/g, "+").replace(/_/g, "/");
    while (base64.length % 4) base64 += "=";
    return decodeURIComponent(escape(atob(base64)));
  }

  function loadContacts() {
    try {
      const raw = localStorage.getItem("gocord.contacts");
      if (!raw) return [];
      const parsed = JSON.parse(raw);
      return Array.isArray(parsed) ? parsed : [];
    } catch (_) {
      return [];
    }
  }

  function saveContacts(contacts) {
    state.contacts = contacts;
    try {
      localStorage.setItem("gocord.contacts", JSON.stringify(contacts));
    } catch (_) {}
    renderContacts();
  }

  function renderContacts() {
    if (!els.contactsList) return;
    if (els.contactsCount) els.contactsCount.textContent = String(state.contacts.length);

    els.contactsList.innerHTML = "";
    if (state.contacts.length === 0) {
      const empty = document.createElement("div");
      empty.className = "contacts-empty-state";
      empty.textContent = "No contacts saved yet. Enable notifications above, generate your Call Card, and exchange cards with friends!";
      els.contactsList.appendChild(empty);
      return;
    }

    state.contacts.forEach((contact) => {
      const item = document.createElement("div");
      item.className = "contact-item";

      const info = document.createElement("div");
      info.className = "contact-info";

      const avatar = document.createElement("div");
      avatar.className = "contact-avatar";
      avatar.textContent = (contact.name || "C").charAt(0).toUpperCase();

      const details = document.createElement("div");
      details.className = "contact-details";

      const name = document.createElement("span");
      name.className = "contact-name";
      name.textContent = contact.name || "Unnamed Contact";

      const meta = document.createElement("span");
      meta.className = "contact-meta";
      meta.textContent = "● PUSH READY";

      details.appendChild(name);
      details.appendChild(meta);
      info.appendChild(avatar);
      info.appendChild(details);

      const actions = document.createElement("div");
      actions.className = "contact-actions";

      const callBtn = document.createElement("button");
      callBtn.className = "btn-brutalist btn-call-contact";
      callBtn.innerHTML = ` + "`" + `
        <svg class="icon-inline" viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.5"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2" ry="2"/></svg>
        <span>CALL</span>
      ` + "`" + `;
      callBtn.addEventListener("click", () => callContact(contact, callBtn));

      const delBtn = document.createElement("button");
      delBtn.className = "btn-brutalist btn-del-contact";
      delBtn.textContent = "✕";
      delBtn.title = "Delete contact";
      delBtn.addEventListener("click", () => deleteContact(contact.id));

      actions.appendChild(callBtn);
      actions.appendChild(delBtn);

      item.appendChild(info);
      item.appendChild(actions);
      els.contactsList.appendChild(item);
    });
  }

  function deleteContact(id) {
    const updated = state.contacts.filter((c) => c.id !== id);
    saveContacts(updated);
  }

  async function callContact(contact, btn) {
    if (!contact?.subscription?.endpoint) {
      showError(els.contactError, "Invalid contact subscription.");
      return;
    }

    const origHTML = btn ? btn.innerHTML : "";
    if (btn) {
      btn.disabled = true;
      btn.textContent = "CALLING...";
    }
    showError(els.contactError, "");

    try {
      // 1. Create encrypted room
      const roomData = await api("/api/rooms", { method: "POST" });
      const joinUrl = roomData.url || ` + "`" + `${location.origin}/join/${roomData.room}#${roomData.secret}` + "`" + `;
      try {
        sessionStorage.setItem(` + "`" + `gocord.secret.${roomData.room}` + "`" + `, roomData.secret);
      } catch (_) {}

      // 2. Send push notification to contact
      const caller = state.myDisplayName || "A contact";
      await api("/api/push/notify", {
        method: "POST",
        body: JSON.stringify({
          subscription: contact.subscription,
          payload: {
            title: ` + "`" + `Call from ${caller}` + "`" + `,
            body: ` + "`" + `${caller} is calling you on Gocord` + "`" + `,
            url: joinUrl,
            room: roomData.room,
            callerName: caller,
          },
          ttl: 120,
        }),
      });

      // 3. Move caller to green room with clean URL
      state.room = roomData.room;
      state.secret = roomData.secret;
      state.inviteURL = joinUrl;
      history.pushState(null, "", ` + "`" + `/join/${encodeURIComponent(state.room)}` + "`" + `);

      if (btn) {
        btn.disabled = false;
        btn.innerHTML = origHTML;
      }
      els.inviteURL.value = state.inviteURL;
      els.inviteBlock.hidden = false;
      updateRoomTags(state.room);
      setStatus(` + "`" + `Calling ${contact.name || "contact"}… waiting for peer to join.` + "`" + `);
      show("precall");
      startPreviewMedia();
    } catch (err) {
      if (btn) {
        btn.disabled = false;
        btn.innerHTML = origHTML;
      }
      if (err.message && (err.message.includes("410") || err.message.includes("VapidPkHashMismatch") || err.message.includes("older server key") || err.message.includes("expired"))) {
        showError(els.contactError, ` + "`" + `${contact.name}'s Call Card was created with an older server key. Ask them to click "REFRESH" on /friends and send you their updated Call Card.` + "`" + `);
      } else {
        showError(els.contactError, ` + "`" + `Could not call contact: ${err.message}` + "`" + `);
      }
    }
  }

  function updatePushUI() {
    const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent) || (navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1);
    const isStandalone = window.matchMedia("(display-mode: standalone)").matches || window.navigator.standalone === true;

    if (els.iosPushBanner) {
      els.iosPushBanner.hidden = !(isIOS && !isStandalone);
    }

    if (!("serviceWorker" in navigator) || !("PushManager" in window)) {
      if (els.pushStatusBadge) {
        els.pushStatusBadge.textContent = "✕ UNSUPPORTED";
        els.pushStatusBadge.className = "push-status-pill pill-error";
      }
      if (els.enablePushBtn) els.enablePushBtn.disabled = true;
      if (els.enablePushBtnText) {
        els.enablePushBtnText.textContent = isIOS ? "INSTALL VIA SHARE ➔ ADD TO HOME SCREEN" : "PUSH NOT SUPPORTED";
      }
      return;
    }

    if (state.pushSubscription) {
      if (els.pushStatusBadge) {
        els.pushStatusBadge.textContent = "● PUSH ACTIVE";
        els.pushStatusBadge.className = "push-status-pill pill-active";
      }
      if (els.enablePushBtnText) els.enablePushBtnText.textContent = "PUSH ACTIVE (CLICK TO REFRESH)";
      if (els.myCardBlock) els.myCardBlock.hidden = false;
      updateCallCardLink();
    } else {
      if (els.pushStatusBadge) {
        els.pushStatusBadge.textContent = "● PUSH INACTIVE";
        els.pushStatusBadge.className = "push-status-pill pill-inactive";
      }
      if (els.enablePushBtnText) els.enablePushBtnText.textContent = "ENABLE CALL NOTIFICATIONS";
      if (els.myCardBlock) els.myCardBlock.hidden = true;
    }
  }

  function updateCallCardLink() {
    if (!state.pushSubscription) return;
    try {
      const subJSON = state.pushSubscription.toJSON();
      const cardObj = {
        name: state.myDisplayName || "",
        endpoint: subJSON.endpoint,
        keys: {
          p256dh: subJSON.keys?.p256dh,
          auth: subJSON.keys?.auth,
        },
      };
      const token = encodeBase64URL(JSON.stringify(cardObj));
      const fullLink = ` + "`" + `${location.origin}/friends#card=${token}` + "`" + `;
      if (els.myCallCardLink) els.myCallCardLink.value = fullLink;
    } catch (_) {}
  }

  async function initServiceWorkerAndPush() {
    if (!("serviceWorker" in navigator) || !("PushManager" in window)) {
      updatePushUI();
      return;
    }

    try {
      state.swRegistration = await navigator.serviceWorker.register("/sw.js", { scope: "/" });
      state.pushSubscription = await state.swRegistration.pushManager.getSubscription();
      updatePushUI();
    } catch (err) {
      console.warn("Service worker / push init error:", err);
      updatePushUI();
    }
  }

  async function subscribePush() {
    showError(els.pushError, "");
    if (!("serviceWorker" in navigator) || !("PushManager" in window)) {
      const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent) || (navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1);
      if (isIOS) {
        showError(els.pushError, "On iOS, tap Share (⎋) ➔ 'Add to Home Screen' first, then open Gocord from your Home Screen.");
      } else {
        showError(els.pushError, "Push notifications are not supported in this browser.");
      }
      return;
    }

    try {
      const permission = await Notification.requestPermission();
      if (permission !== "granted") {
        showError(els.pushError, "Notification permission was denied. Please allow notifications in browser site settings.");
        return;
      }

      if (!state.config) {
        state.config = await api("/api/config");
      }

      const vapidKey = state.config?.vapidPublicKey;
      if (!vapidKey) {
        showError(els.pushError, "Could not obtain VAPID public key from server.");
        return;
      }

      const reg = state.swRegistration || (await navigator.serviceWorker.ready);
      
      // Unsubscribe any prior subscription to prevent "different application server key already exists" error
      try {
        const existingSub = await reg.pushManager.getSubscription();
        if (existingSub) {
          await existingSub.unsubscribe();
        }
      } catch (_) {}

      const convertedVapidKey = urlBase64ToUint8Array(vapidKey);
      state.pushSubscription = await reg.pushManager.subscribe({
        userVisibleOnly: true,
        applicationServerKey: convertedVapidKey,
      });

      updatePushUI();
      playSound("join");
      if (els.contactSuccess) {
        els.contactSuccess.textContent = "✦ Call Card updated with current server key! Share your new link.";
        els.contactSuccess.hidden = false;
        setTimeout(() => {
          if (els.contactSuccess) els.contactSuccess.hidden = true;
        }, 4000);
      }
    } catch (err) {
      showError(els.pushError, ` + "`" + `Failed to subscribe: ${err.message}` + "`" + `);
    }
  }

  function parseCallCard(raw) {
    let input = raw.trim();
    if (input.includes("#card=")) {
      input = input.split("#card=")[1];
    } else if (input.startsWith("#card=")) {
      input = input.slice(6);
    }

    let parsed = null;
    try {
      const decoded = decodeBase64URL(input);
      parsed = JSON.parse(decoded);
    } catch (_) {
      try {
        parsed = JSON.parse(input);
      } catch (__) {}
    }

    if (!parsed) {
      throw new Error("Could not decode Call Card format.");
    }

    let endpoint = parsed.endpoint || parsed.subscription?.endpoint;
    let p256dh = parsed.keys?.p256dh || parsed.subscription?.keys?.p256dh;
    let auth = parsed.keys?.auth || parsed.subscription?.keys?.auth;
    let name = parsed.name || "";

    if (!endpoint || !p256dh || !auth) {
      throw new Error("Call Card is missing endpoint or cryptographic keys.");
    }

    return {
      name,
      subscription: {
        endpoint,
        keys: { p256dh, auth },
      },
    };
  }

  function handleSaveContact() {
    showError(els.contactError, "");
    if (els.contactSuccess) els.contactSuccess.hidden = true;

    const name = els.contactNameInput ? els.contactNameInput.value.trim() : "";
    const rawCard = els.contactCardInput ? els.contactCardInput.value.trim() : "";

    if (!name) {
      showError(els.contactError, "Please enter a contact name.");
      return;
    }
    if (!rawCard) {
      showError(els.contactError, "Please paste a Call Card link or code.");
      return;
    }

    try {
      const card = parseCallCard(rawCard);
      const contactObj = {
        id: ` + "`" + `c_${Date.now()}_${Math.random().toString(36).slice(2, 7)}` + "`" + `,
        name: name,
        subscription: card.subscription,
        addedAt: Date.now(),
      };

      const existingIdx = state.contacts.findIndex(
        (c) => c.subscription.endpoint === card.subscription.endpoint || c.name.toLowerCase() === name.toLowerCase()
      );

      let updated = [...state.contacts];
      if (existingIdx >= 0) {
        updated[existingIdx] = contactObj;
      } else {
        updated.push(contactObj);
      }

      saveContacts(updated);
      if (els.contactNameInput) els.contactNameInput.value = "";
      if (els.contactCardInput) els.contactCardInput.value = "";
      if (els.contactSuccess) {
        els.contactSuccess.textContent = ` + "`" + `✦ Contact "${contactObj.name}" saved successfully!` + "`" + `;
        els.contactSuccess.hidden = false;
        setTimeout(() => {
          if (els.contactSuccess) els.contactSuccess.hidden = true;
        }, 4000);
      }
      playSound("join");
    } catch (err) {
      showError(els.contactError, err.message);
    }
  }

  function handleCardHashImport() {
    if (location.hash && location.hash.startsWith("#card=")) {
      try {
        const card = parseCallCard(location.hash);
        if (els.contactNameInput && card.name) els.contactNameInput.value = card.name;
        if (els.contactCardInput) els.contactCardInput.value = location.href;
        if (els.contactSuccess) {
          els.contactSuccess.textContent = ` + "`" + `✦ Call Card detected${card.name ? ` + "`" + ` for "${card.name}"` + "`" + ` : ""}! Verify name and click "SAVE CONTACT".` + "`" + `;
          els.contactSuccess.hidden = false;
        }
        show("friends");
        history.replaceState(null, "", "/friends");
      } catch (_) {}
    }
  }

  async function copyCallCardLink() {
    if (!els.myCallCardLink || !els.myCallCardLink.value) return;
    try {
      await navigator.clipboard.writeText(els.myCallCardLink.value);
      if (els.copyCallCardBtn) {
        const orig = els.copyCallCardBtn.innerHTML;
        els.copyCallCardBtn.textContent = "COPIED!";
        setTimeout(() => { if (els.copyCallCardBtn) els.copyCallCardBtn.innerHTML = orig; }, 2000);
      }
    } catch (_) {
      els.myCallCardLink.select();
    }
  }

  function navigate(path, replace = false) {
    if (replace) {
      history.replaceState(null, "", path);
    } else {
      history.pushState(null, "", path);
    }
    handleRoute();
  }

  async function handleRoute() {
    handleCardHashImport();

    const path = location.pathname.toLowerCase();
    if (path === "/friends" || path === "/contacts") {
      show("friends");
      return;
    }

    const locationRoom = parseRoomFromLocation();
    if (!locationRoom) {
      if (!location.hash.startsWith("#card=")) {
        show("home");
      }
      return;
    }

    state.room = locationRoom.room;
    state.secret = locationRoom.secret;
    state.inviteURL = ` + "`" + `${location.origin}/join/${state.room}#${state.secret}` + "`" + `;
    show("precall");
    updateRoomTags(state.room);

    if (!state.secret) {
      showError(els.preCallError, "This invitation is missing its #secret fragment. Ask for the complete link.");
      els.startButton.disabled = true;
      return;
    }

    try {
      setStatus("Checking room availability…");
      await api(` + "`" + `/api/rooms/${encodeURIComponent(state.room)}` + "`" + `);
      setStatus("Room available. Dual-stack P2P ready.");
      els.inviteURL.value = state.inviteURL;
      els.inviteBlock.hidden = false;
      startPreviewMedia();
    } catch (err) {
      showError(els.preCallError, err.message === "Request failed (404)" ? "This room does not exist or has expired." : err.message);
      els.startButton.disabled = true;
    }
  }

  async function initialize() {
    state.contacts = loadContacts();
    bindUI();
    await enumerateHardwareDevices();

    if (els.myDisplayName) {
      els.myDisplayName.value = state.myDisplayName;
    }
    renderContacts();
    initServiceWorkerAndPush();
    await handleRoute();
  }

  function updateRoomTags(room) {
    const display = ` + "`" + `#${room.slice(0, 8)}` + "`" + `;
    if (els.preCallRoomBadge) els.preCallRoomBadge.textContent = ` + "`" + `✦ GOCORD READY: ${display}` + "`" + `;
    if (els.callRoomName) els.callRoomName.textContent = ` + "`" + `ROOM ${display}` + "`" + `;
  }

  function bindUI() {
    // Screen 1: Lobby
    if (els.createButton) els.createButton.addEventListener("click", createCall);
    if (els.quickStartBtn) els.quickStartBtn.addEventListener("click", createCall);
    if (els.joinButton) els.joinButton.addEventListener("click", handleJoinInput);
    if (els.joinInput) {
      els.joinInput.addEventListener("keydown", (e) => {
        if (e.key === "Enter") handleJoinInput();
      });
    }
    if (els.openFriendsBtn) els.openFriendsBtn.addEventListener("click", () => navigate("/friends"));
    if (els.navFriendsBtn) els.navFriendsBtn.addEventListener("click", () => navigate("/friends"));
    if (els.mobileNavFriendsBtn) els.mobileNavFriendsBtn.addEventListener("click", () => navigate("/friends"));
    if (els.backToHomeFromFriendsBtn) els.backToHomeFromFriendsBtn.addEventListener("click", () => navigate("/"));

    // Push & Contacts UI
    if (els.enablePushBtn) els.enablePushBtn.addEventListener("click", subscribePush);
    if (els.copyCallCardBtn) els.copyCallCardBtn.addEventListener("click", copyCallCardLink);
    if (els.saveContactBtn) els.saveContactBtn.addEventListener("click", handleSaveContact);
    if (els.contactCardInput) {
      els.contactCardInput.addEventListener("keydown", (e) => {
        if (e.key === "Enter") handleSaveContact();
      });
    }
    if (els.myDisplayName) {
      els.myDisplayName.addEventListener("input", (e) => {
        state.myDisplayName = e.target.value.trim();
        try {
          localStorage.setItem("gocord.displayName", state.myDisplayName);
        } catch (_) {}
        updateCallCardLink();
      });
    }

    // Screen 2: Pre-call
    if (els.copyButton) els.copyButton.addEventListener("click", copyInvite);
    if (els.callRoomBadge) els.callRoomBadge.addEventListener("click", copyInvite);
    if (els.startButton) els.startButton.addEventListener("click", startSession);
    if (els.precallCreateRoomNavBtn) els.precallCreateRoomNavBtn.addEventListener("click", createCall);
    if (els.precallCreateNewBtn) els.precallCreateNewBtn.addEventListener("click", createCall);
    if (els.precallMicToggle) els.precallMicToggle.addEventListener("click", togglePrecallMic);
    if (els.precallCamToggle) els.precallCamToggle.addEventListener("click", togglePrecallCam);
    if (els.precallNoiseToggle) els.precallNoiseToggle.addEventListener("click", togglePrecallNoise);

    if (els.audioSource) els.audioSource.addEventListener("change", switchAudioInput);
    if (els.videoSource) els.videoSource.addEventListener("change", switchVideoInput);
    if (els.audioOutput) els.audioOutput.addEventListener("change", switchAudioOutput);

    // Screen 3: In-call Controls
    if (els.muteButton) els.muteButton.addEventListener("click", toggleMute);
    if (els.cameraButton) els.cameraButton.addEventListener("click", toggleCamera);
    if (els.shareScreenButton) {
      if (!navigator.mediaDevices || !navigator.mediaDevices.getDisplayMedia) {
        els.shareScreenButton.style.display = "none";
      } else {
        els.shareScreenButton.addEventListener("click", toggleScreenShare);
      }
    }
    if (els.voiceFxButton) els.voiceFxButton.addEventListener("click", toggleVoiceFx);
    if (els.switchCameraButton) els.switchCameraButton.addEventListener("click", switchCamera);
    if (els.toggleDrawerButton) els.toggleDrawerButton.addEventListener("click", toggleDrawer);
    if (els.closeDrawerButton) els.closeDrawerButton.addEventListener("click", toggleDrawer);
    if (els.hangupButton) els.hangupButton.addEventListener("click", () => hangup(true));

    if (els.drawerAudioSource) els.drawerAudioSource.addEventListener("change", switchAudioInput);
    if (els.drawerAudioOutput) els.drawerAudioOutput.addEventListener("change", switchAudioOutput);
    if (els.drawerVideoSource) els.drawerVideoSource.addEventListener("change", switchVideoInput);

    // Quality Profile Buttons
    document.querySelectorAll(".btn-quality-pill:not(.btn-fit-pill)").forEach(btn => {
      btn.addEventListener("click", (e) => {
        document.querySelectorAll(".btn-quality-pill:not(.btn-fit-pill)").forEach(b => b.classList.remove("active"));
        e.currentTarget.classList.add("active");
        const q = e.currentTarget.dataset.quality;
        if (els.qualitySelect) els.qualitySelect.value = q;
        applyQualityProfile(q);
      });
    });

    // Video Fit Buttons
    document.querySelectorAll(".btn-fit-pill").forEach(btn => {
      btn.addEventListener("click", (e) => {
        document.querySelectorAll(".btn-fit-pill").forEach(b => b.classList.remove("active"));
        e.currentTarget.classList.add("active");
        const fit = e.currentTarget.dataset.fit;
        if (els.remoteVideo) els.remoteVideo.style.objectFit = fit;
        if (els.localVideo) els.localVideo.style.objectFit = fit;
      });
    });

    if (els.playRemoteButton) {
      els.playRemoteButton.addEventListener("click", async () => {
        try {
          await els.remoteVideo.play();
          els.playRemoteButton.hidden = true;
        } catch (_) {}
      });
    }

    if (els.remoteVideo) els.remoteVideo.addEventListener("loadedmetadata", playRemote);
    window.addEventListener("beforeunload", () => closeLocalResources(false));
    navigator.mediaDevices?.addEventListener?.("devicechange", enumerateHardwareDevices);
    initDraggablePip();
  }

  // ==========================================
  // HARDWARE DEVICES & PREVIEW AUDIO VISUALIZER
  // ==========================================

  async function enumerateHardwareDevices() {
    if (!navigator.mediaDevices?.enumerateDevices) return;
    try {
      const devices = await navigator.mediaDevices.enumerateDevices();
      populateSelect(els.audioSource, devices, "audioinput", "Default Microphone");
      populateSelect(els.drawerAudioSource, devices, "audioinput", "Default Microphone");
      populateSelect(els.audioOutput, devices, "audiooutput", "Default Speaker");
      populateSelect(els.drawerAudioOutput, devices, "audiooutput", "Default Speaker");
      populateSelect(els.videoSource, devices, "videoinput", "Default Camera");
      populateSelect(els.drawerVideoSource, devices, "videoinput", "Default Camera");
    } catch (_) {}
  }

  function populateSelect(selectEl, devices, kind, defaultLabel) {
    if (!selectEl) return;
    const current = selectEl.value;
    selectEl.innerHTML = "";
    const filtered = devices.filter(d => d.kind === kind);
    if (filtered.length === 0) {
      const opt = document.createElement("option");
      opt.value = "";
      opt.textContent = defaultLabel;
      selectEl.appendChild(opt);
      return;
    }
    filtered.forEach((d, idx) => {
      const opt = document.createElement("option");
      opt.value = d.deviceId;
      opt.textContent = d.label || ` + "`" + `${defaultLabel} ${idx + 1}` + "`" + `;
      selectEl.appendChild(opt);
    });
    if (current && [...selectEl.options].some(o => o.value === current)) {
      selectEl.value = current;
    }
  }

  async function startPreviewMedia() {
    stopPreviewMedia();
    try {
      const constraints = {
        video: state.camEnabled ? {
          ...videoConstraints,
          deviceId: els.videoSource?.value ? { exact: els.videoSource.value } : undefined,
          facingMode: { ideal: state.facingMode }
        } : false,
        audio: state.micEnabled ? {
          deviceId: els.audioSource?.value ? { exact: els.audioSource.value } : undefined,
          echoCancellation: { ideal: true },
          noiseSuppression: { ideal: state.noiseSuppression },
        } : false,
      };

      if (!constraints.video && !constraints.audio) return;
      state.previewStream = await navigator.mediaDevices.getUserMedia(constraints);
      
      if (els.previewVideo && constraints.video) {
        els.previewVideo.srcObject = state.previewStream;
        if (els.previewFallback) els.previewFallback.hidden = true;
      }

      if (constraints.audio) {
        attachAudioAnalyser(state.previewStream, els.precallVuMeter);
      }
      await enumerateHardwareDevices();
    } catch (err) {
      if (els.previewFallback) els.previewFallback.hidden = false;
    }
  }

  function stopPreviewMedia() {
    if (state.previewStream) {
      state.previewStream.getTracks().forEach(t => t.stop());
      state.previewStream = null;
    }
    stopAudioAnalyser();
  }

  function attachAudioAnalyser(stream, meterEl) {
    if (!stream || stream.getAudioTracks().length === 0 || !meterEl) return;
    try {
      const AudioCtx = window.AudioContext || window.webkitAudioContext;
      if (!AudioCtx) return;
      if (!state.audioContext) state.audioContext = new AudioCtx();
      if (state.audioContext.state === "suspended") state.audioContext.resume();

      const source = state.audioContext.createMediaStreamSource(stream);
      state.analyser = state.audioContext.createAnalyser();
      state.analyser.fftSize = 32;
      source.connect(state.analyser);

      const bufferLength = state.analyser.frequencyBinCount;
      const dataArray = new Uint8Array(bufferLength);
      const bars = meterEl.querySelectorAll(".bar");

      function updateVU() {
        if (!state.analyser) return;
        state.analyser.getByteFrequencyData(dataArray);
        let sum = 0;
        for (let i = 0; i < bufferLength; i++) sum += dataArray[i];
        const average = sum / bufferLength;
        const activeCount = Math.min(bars.length, Math.round((average / 128) * bars.length));

        bars.forEach((bar, idx) => {
          if (idx < activeCount) {
            bar.classList.add("active");
            bar.style.height = ` + "`" + `${Math.max(6, Math.min(14, (dataArray[idx] || average) / 18))}px` + "`" + `;
          } else {
            bar.classList.remove("active");
            bar.style.height = "4px";
          }
        });

        state.animFrameId = requestAnimationFrame(updateVU);
      }

      updateVU();
    } catch (_) {}
  }

  function stopAudioAnalyser() {
    if (state.animFrameId) {
      cancelAnimationFrame(state.animFrameId);
      state.animFrameId = null;
    }
    if (state.analyser) state.analyser = null;
  }

  // ==========================================
  // CALL CREATION & ROUTING
  // ==========================================

  async function createCall() {
    if (els.createButton) els.createButton.disabled = true;
    if (els.quickStartBtn) els.quickStartBtn.disabled = true;
    showError(els.homeError, "");
    try {
      const created = await api("/api/rooms", { method: "POST", body: "{}" });
      state.room = created.room;
      state.secret = created.secret;
      state.inviteURL = created.url || ` + "`" + `${location.origin}/join/${state.room}#${state.secret}` + "`" + `;
      try {
        sessionStorage.setItem(` + "`" + `gocord.secret.${state.room}` + "`" + `, state.secret);
      } catch (_) {}

      // Occult the secret from the address bar
      history.pushState(null, "", ` + "`" + `/join/${encodeURIComponent(state.room)}` + "`" + `);

      els.inviteURL.value = state.inviteURL;
      els.inviteBlock.hidden = false;
      updateRoomTags(state.room);
      setStatus("Room ready. Share invite link and check media.");
      show("precall");
      startPreviewMedia();
    } catch (err) {
      showError(els.homeError, err.message);
    } finally {
      if (els.createButton) els.createButton.disabled = false;
      if (els.quickStartBtn) els.quickStartBtn.disabled = false;
    }
  }

  function handleJoinInput() {
    const val = els.joinInput?.value?.trim();
    if (!val) return;
    try {
      if (val.startsWith("http")) {
        const url = new URL(val);
        const match = url.pathname.match(/^\/join\/([A-Za-z0-9_-]+)$/);
        if (match) {
          const room = match[1];
          const secret = url.hash.slice(1);
          if (secret) {
            try {
              sessionStorage.setItem(` + "`" + `gocord.secret.${room}` + "`" + `, secret);
            } catch (_) {}
          }
          state.room = room;
          state.secret = secret;
          state.inviteURL = url.href;
          history.pushState(null, "", ` + "`" + `/join/${encodeURIComponent(room)}` + "`" + `);
          show("precall");
          updateRoomTags(state.room);
          els.inviteURL.value = state.inviteURL;
          els.inviteBlock.hidden = false;
          startPreviewMedia();
          return;
        }
      }

      let room = val;
      let secret = "";
      if (val.includes("#")) {
        const parts = val.split("#");
        room = parts[0];
        secret = parts[1];
      }
      if (secret) {
        try {
          sessionStorage.setItem(` + "`" + `gocord.secret.${room}` + "`" + `, secret);
        } catch (_) {}
      }
      state.room = room;
      state.secret = secret;
      state.inviteURL = ` + "`" + `${location.origin}/join/${room}#${secret}` + "`" + `;
      history.pushState(null, "", ` + "`" + `/join/${encodeURIComponent(room)}` + "`" + `);
      show("precall");
      updateRoomTags(state.room);
      els.inviteURL.value = state.inviteURL;
      els.inviteBlock.hidden = false;
      startPreviewMedia();
    } catch (_) {
      location.href = ` + "`" + `/join/${encodeURIComponent(val)}` + "`" + `;
    }
  }

  async function copyInvite() {
    const url = state.inviteURL || els.inviteURL?.value || ` + "`" + `${location.origin}/join/${state.room}#${state.secret}` + "`" + `;
    try {
      await navigator.clipboard.writeText(url);
      const btn = els.copyButton;
      if (btn) {
        const old = btn.innerHTML;
        btn.textContent = "COPIED!";
        setTimeout(() => { btn.innerHTML = old; }, 1400);
      }
      if (els.callRoomName) {
        const oldName = els.callRoomName.textContent;
        els.callRoomName.textContent = "COPIED INVITE!";
        setTimeout(() => { els.callRoomName.textContent = oldName; }, 1400);
      }
    } catch (_) {
      if (els.inviteURL) {
        els.inviteURL.value = url;
        els.inviteURL.select();
        document.execCommand("copy");
      }
    }
  }

  // ==========================================
  // PRE-CALL TOGGLES & HARDWARE SWITCHERS
  // ==========================================

  function togglePrecallMic() {
    state.micEnabled = !state.micEnabled;
    els.precallMicToggle.classList.toggle("active", state.micEnabled);
    if (els.precallMicLabel) els.precallMicLabel.textContent = state.micEnabled ? "MIC ACTIVE" : "MIC MUTED";
    if (state.previewStream) {
      state.previewStream.getAudioTracks().forEach(t => { t.enabled = state.micEnabled; });
    }
  }

  function togglePrecallCam() {
    state.camEnabled = !state.camEnabled;
    els.precallCamToggle.classList.toggle("active", state.camEnabled);
    if (els.precallCamLabel) els.precallCamLabel.textContent = state.camEnabled ? "CAM ACTIVE" : "CAM OFF";
    if (state.previewStream) {
      state.previewStream.getVideoTracks().forEach(t => { t.enabled = state.camEnabled; });
      if (els.previewFallback) els.previewFallback.hidden = state.camEnabled;
    }
  }

  async function togglePrecallNoise() {
    state.noiseSuppression = !state.noiseSuppression;
    els.precallNoiseToggle.classList.toggle("active", state.noiseSuppression);
    if (state.previewStream) {
      const oldAudioTrack = state.previewStream.getAudioTracks()[0];
      try {
        const newStream = await navigator.mediaDevices.getUserMedia({
          audio: {
            deviceId: els.audioSource?.value ? { exact: els.audioSource.value } : undefined,
            echoCancellation: { ideal: true },
            noiseSuppression: { ideal: state.noiseSuppression },
          }
        });
        const newTrack = newStream.getAudioTracks()[0];
        newTrack.enabled = state.micEnabled;
        if (oldAudioTrack) {
          oldAudioTrack.stop();
          state.previewStream.removeTrack(oldAudioTrack);
        }
        state.previewStream.addTrack(newTrack);
        attachAudioAnalyser(state.previewStream, els.precallVuMeter);
      } catch (_) {}
    }
  }

  async function switchAudioInput(e) {
    const deviceId = e.target.value;
    if (els.audioSource) els.audioSource.value = deviceId;
    if (els.drawerAudioSource) els.drawerAudioSource.value = deviceId;
    if (state.started && state.localStream) {
      const audioTrack = state.localStream.getAudioTracks()[0];
      if (audioTrack) audioTrack.stop();
      const newStream = await navigator.mediaDevices.getUserMedia({ audio: { deviceId: { exact: deviceId } } });
      const newTrack = newStream.getAudioTracks()[0];
      state.localStream.removeTrack(audioTrack);
      state.localStream.addTrack(newTrack);
      const sender = state.pc?.getSenders?.().find(s => s.track?.kind === "audio");
      if (sender) sender.replaceTrack(newTrack);
    } else if (state.previewStream) {
      const oldAudioTrack = state.previewStream.getAudioTracks()[0];
      try {
        const newStream = await navigator.mediaDevices.getUserMedia({ audio: { deviceId: { exact: deviceId } } });
        const newTrack = newStream.getAudioTracks()[0];
        newTrack.enabled = state.micEnabled;
        if (oldAudioTrack) {
          oldAudioTrack.stop();
          state.previewStream.removeTrack(oldAudioTrack);
        }
        state.previewStream.addTrack(newTrack);
        attachAudioAnalyser(state.previewStream, els.precallVuMeter);
      } catch (_) {}
    }
  }

  async function switchVideoInput(e) {
    const deviceId = e.target.value;
    if (els.videoSource) els.videoSource.value = deviceId;
    if (els.drawerVideoSource) els.drawerVideoSource.value = deviceId;
    if (state.started && state.localStream) {
      const videoTrack = state.localStream.getVideoTracks()[0];
      if (videoTrack) videoTrack.stop();
      const newStream = await navigator.mediaDevices.getUserMedia({
        video: { ...videoConstraints, deviceId: { exact: deviceId } }
      });
      const newTrack = newStream.getVideoTracks()[0];
      state.localStream.removeTrack(videoTrack);
      state.localStream.addTrack(newTrack);
      const sender = state.pc?.getSenders?.().find(s => s.track?.kind === "video");
      if (sender) sender.replaceTrack(newTrack);
      els.localVideo.srcObject = state.localStream;
    } else {
      startPreviewMedia();
    }
  }

  async function switchAudioOutput(e) {
    const deviceId = e.target.value;
    if (els.audioOutput) els.audioOutput.value = deviceId;
    if (els.drawerAudioOutput) els.drawerAudioOutput.value = deviceId;
    if (els.remoteVideo?.setSinkId) {
      try { await els.remoteVideo.setSinkId(deviceId); } catch (_) {}
    }
  }

  // ==========================================
  // ACTIVE CALL SESSION
  // ==========================================

  async function startSession() {
    if (state.started) return;
    state.started = true;
    state.intentionalClose = false;
    state.terminal = false;
    state.reconnectAttempts = 0;
    state.iceRestartInFlight = false;
    els.startButton.disabled = true;
    showError(els.preCallError, "");

    stopPreviewMedia();

    try {
      setStatus("Initializing signaling…");
      state.config = await api("/api/config");

      setStatus("Acquiring media streams…");
      state.localStream = await navigator.mediaDevices.getUserMedia({
        video: state.camEnabled ? {
          ...videoConstraints,
          deviceId: els.videoSource?.value ? { exact: els.videoSource.value } : undefined,
          facingMode: { ideal: state.facingMode }
        } : false,
        audio: state.micEnabled ? {
          deviceId: els.audioSource?.value ? { exact: els.audioSource.value } : undefined,
          echoCancellation: { ideal: true },
          noiseSuppression: { ideal: state.noiseSuppression },
          autoGainControl: { ideal: true },
        } : false,
      });

      const videoTrack = state.localStream.getVideoTracks()[0];
      if (videoTrack && "contentHint" in videoTrack) videoTrack.contentHint = "motion";
      if (els.localVideo) {
        els.localVideo.srcObject = state.localStream;
        els.localVideo.addEventListener("loadedmetadata", () => {
          if (els.localVideo.videoWidth && els.localVideo.videoHeight && els.selfPipCard) {
            els.selfPipCard.style.aspectRatio = ` + "`" + `${els.localVideo.videoWidth} / ${els.localVideo.videoHeight}` + "`" + `;
          }
        });
      }

      if (state.micEnabled) {
        attachAudioAnalyser(state.localStream, els.pipVuMeter);
      }

      if (window.innerWidth < 768 && els.telemetryDrawer) {
        els.telemetryDrawer.classList.add("collapsed");
        if (els.toggleDrawerButton) els.toggleDrawerButton.setAttribute("aria-expanded", "false");
      }

      show("call");
      setStatus("Connecting peer mesh…");
      startCallTimer();
      createPeerConnection();
      connectWebSocket();
      startStats();
    } catch (err) {
      state.started = false;
      els.startButton.disabled = false;
      show("precall");
      showError(els.preCallError, friendlyMediaError(err));
      closeLocalResources(false);
    }
  }

  function friendlyMediaError(err) {
    if (err?.name === "NotAllowedError") return "Camera or microphone permission was denied.";
    if (err?.name === "NotFoundError") return "No usable camera or microphone was found.";
    if (err?.name === "NotReadableError") return "Hardware in use by another application.";
    return err?.message || "Could not start call.";
  }

  function startCallTimer() {
    state.callStartTime = Date.now();
    if (state.callDurationTimer) clearInterval(state.callDurationTimer);
    state.callDurationTimer = setInterval(() => {
      const elapsed = Math.floor((Date.now() - state.callStartTime) / 1000);
      const mins = String(Math.floor(elapsed / 60)).padStart(2, "0");
      const secs = String(elapsed % 60).padStart(2, "0");
      if (els.callDurationText) els.callDurationText.textContent = ` + "`" + `LIVE [${mins}:${secs}]` + "`" + `;
    }, 1000);
  }

  function toggleDrawer() {
    if (!els.telemetryDrawer) return;
    const isCollapsed = els.telemetryDrawer.classList.toggle("collapsed");
    if (els.toggleDrawerButton) {
      els.toggleDrawerButton.setAttribute("aria-expanded", String(!isCollapsed));
    }
  }

  function toggleMute() {
    if (!state.localStream) return;
    const track = state.localStream.getAudioTracks()[0];
    if (!track) return;
    track.enabled = !track.enabled;
    const muted = !track.enabled;
    els.muteButton.setAttribute("aria-pressed", String(muted));
    els.muteButton.querySelector(".dock-label").textContent = muted ? "MIC OFF" : "MIC ON";
    playSound(muted ? "mute" : "unmute");
  }

  function toggleCamera() {
    if (!state.localStream) return;
    const track = state.localStream.getVideoTracks()[0];
    if (!track) return;
    track.enabled = !track.enabled;
    const disabled = !track.enabled;
    els.cameraButton.setAttribute("aria-pressed", String(disabled));
    els.cameraButton.querySelector(".dock-label").textContent = disabled ? "CAM OFF" : "CAM ON";
  }

  async function toggleScreenShare() {
    if (state.isSharingScreen) {
      // Revert to camera
      state.isSharingScreen = false;
      els.shareScreenButton.setAttribute("aria-pressed", "false");
      els.localVideo.classList.remove("no-mirror");
      if (state.screenStream) {
        state.screenStream.getTracks().forEach(t => t.stop());
        state.screenStream = null;
      }
      const camStream = await navigator.mediaDevices.getUserMedia({ video: videoConstraints });
      const camTrack = camStream.getVideoTracks()[0];
      const sender = state.pc?.getSenders?.().find(s => s.track?.kind === "video");
      if (sender) sender.replaceTrack(camTrack);
      els.localVideo.srcObject = camStream;
      state.localStream = camStream;
    } else {
      // Start screen share
      try {
        state.screenStream = await navigator.mediaDevices.getDisplayMedia({ video: { cursor: "always" }, audio: false });
        const screenTrack = state.screenStream.getVideoTracks()[0];
        screenTrack.onended = () => toggleScreenShare();
        const sender = state.pc?.getSenders?.().find(s => s.track?.kind === "video");
        if (sender) sender.replaceTrack(screenTrack);
        els.localVideo.srcObject = state.screenStream;
        els.localVideo.classList.add("no-mirror");
        state.isSharingScreen = true;
        els.shareScreenButton.setAttribute("aria-pressed", "true");
      } catch (_) {}
    }
  }

  function toggleVoiceFx() {
    const isPressed = els.voiceFxButton.getAttribute("aria-pressed") === "true";
    els.voiceFxButton.setAttribute("aria-pressed", String(!isPressed));
  }

  async function switchCamera() {
    state.facingMode = state.facingMode === "user" ? "environment" : "user";
    if (!state.localStream) return;
    try {
      const oldTrack = state.localStream.getVideoTracks()[0];
      if (oldTrack) oldTrack.stop();
      const nextStream = await navigator.mediaDevices.getUserMedia({
        video: { ...videoConstraints, facingMode: { exact: state.facingMode } },
      });
      const nextTrack = nextStream.getVideoTracks()[0];
      state.localStream.removeTrack(oldTrack);
      state.localStream.addTrack(nextTrack);
      els.localVideo.srcObject = state.localStream;
      const sender = state.pc?.getSenders?.().find(s => s.track?.kind === "video");
      if (sender) sender.replaceTrack(nextTrack);
    } catch (_) {}
  }

  // ==========================================
  // WEBRTC PEER CONNECTION & SIGNALING
  // ==========================================

  function createPeerConnection() {
    if (state.pc) state.pc.close();
    state.pendingCandidates = [];
    state.selectedPairVerified = false;
    state.pc = new RTCPeerConnection({
      iceServers: state.config?.iceServers || [],
      bundlePolicy: "max-bundle",
      rtcpMuxPolicy: "require",
      iceTransportPolicy: "all",
      // Pre-gather a small pool during the green room so the first offer already
      // carries candidates instead of trickling them after negotiation starts.
      iceCandidatePoolSize: 4,
    });

    for (const track of state.localStream.getTracks()) {
      state.pc.addTrack(track, state.localStream);
    }

    // Ordering must happen before the first createOffer, so do it as soon as
    // the video transceiver exists.
    preferVideoCodec(state.pc);
    configureRtpPriorities(state.pc);

    state.pc.onicecandidate = ({ candidate }) => {
      if (!candidate || !state.ws || state.ws.readyState !== WebSocket.OPEN) return;
      sendMessage({
        type: "ice-candidate",
        room: state.room,
        payload: {
          candidate: candidate.candidate,
          sdpMid: candidate.sdpMid,
          sdpMLineIndex: candidate.sdpMLineIndex,
          usernameFragment: candidate.usernameFragment,
        }
      });
    };

    state.pc.ontrack = ({ streams }) => {
      state.remoteStream = streams[0];
      els.remoteVideo.srcObject = state.remoteStream;
      if (els.remotePlaceholder) els.remotePlaceholder.hidden = true;
      setStatus("Connected (P2P)");
      if (els.remotePeerName) els.remotePeerName.textContent = "PEER CONNECTED";
    };

    state.pc.onconnectionstatechange = () => {
      if (!state.pc) return;
      onMediaStateChange(state.pc.connectionState);
    };

    // Older Safari does not fire connectionstatechange reliably; iceConnectionState
    // covers the same transitions there.
    state.pc.oniceconnectionstatechange = () => {
      if (!state.pc) return;
      const ice = state.pc.iceConnectionState;
      if (ice === "failed" || ice === "disconnected") onMediaStateChange(ice);
    };
  }

  // ==========================================
  // CONNECTION RECOVERY
  // ==========================================

  function onMediaStateChange(mediaState) {
    if (mediaState === "connected" || mediaState === "completed") {
      clearTimer("disconnectTimer");
      state.iceRestartInFlight = false;
      setStatus("Connected");
      if (els.remotePlaceholder) els.remotePlaceholder.hidden = true;
      if (els.remotePeerName) els.remotePeerName.textContent = "PEER CONNECTED";
      playSound("join");
      return;
    }
    if (mediaState !== "disconnected" && mediaState !== "failed") return;

    if (els.remotePlaceholder) els.remotePlaceholder.hidden = false;

    // A brief "disconnected" is common on network handover and usually recovers
    // without intervention, so give it a grace period before forcing a restart.
    // "failed" never recovers on its own.
    const failed = mediaState === "failed";
    const grace = failed ? 0 : ICE_DISCONNECT_GRACE_MS;
    setStatus(failed ? "Media failed, retrying…" : "Connection unstable…");

    // "failed" is definitive, so it supersedes a grace timer still waiting on a
    // "disconnected" that turned out not to heal.
    if (state.disconnectTimer) {
      if (!failed) return;
      clearTimer("disconnectTimer");
    }
    state.disconnectTimer = setTimeout(() => {
      state.disconnectTimer = null;
      if (!state.pc) return;
      const current = state.pc.connectionState;
      if (current === "connected" || current === "completed") return; // healed itself
      attemptIceRestart();
    }, grace);
  }

  // Rebuilds the candidate pair without tearing down the call. Only the offering
  // side may do this: this signaling flow has a single offerer, so a restart
  // from the callee would collide with the caller's negotiation.
  async function attemptIceRestart() {
    if (!state.pc || state.iceRestartInFlight || state.terminal) return;
    if (state.role !== "caller") {
      setStatus("Connection lost, waiting for peer to re-establish…");
      return;
    }
    if (!state.ws || state.ws.readyState !== WebSocket.OPEN) {
      // Nothing to carry the offer. The signaling reconnect re-offers on rejoin.
      setStatus("Waiting for signaling before retrying media…");
      return;
    }
    state.iceRestartInFlight = true;
    setStatus("Re-establishing media…");
    try {
      await makeOffer({ iceRestart: true });
    } finally {
      state.iceRestartInFlight = false;
    }
  }

  function clearTimer(name) {
    if (state[name]) {
      clearTimeout(state[name]);
      state[name] = null;
    }
  }

  // The peer's socket dropped, but the server keeps the room alive for its grace
  // window, so treat this as recoverable until the window closes.
  function startPeerReturnTimer() {
    clearTimer("peerGoneTimer");
    state.peerGoneTimer = setTimeout(() => {
      state.peerGoneTimer = null;
      setStatus("Peer did not return");
      if (els.remotePeerName) els.remotePeerName.textContent = "PEER LEFT THE CALL";
    }, PEER_RETURN_TIMEOUT_MS);
  }

  function onPeerPresent() {
    clearTimer("peerGoneTimer");
    state.reconnectAttempts = 0;
  }

  function scheduleSignalingReconnect() {
    if (state.intentionalClose || state.terminal || !state.started) return;
    if (state.reconnectTimer) return;

    // Exponential backoff with jitter, so two peers that dropped together do not
    // retry in lockstep.
    const attempt = state.reconnectAttempts++;
    const backoff = Math.min(SIGNALING_RETRY_MAX_MS, SIGNALING_RETRY_BASE_MS * 2 ** attempt);
    const delay = Math.round(backoff * (0.5 + Math.random() * 0.5));

    setStatus(` + "`" + `Signaling lost, retrying in ${Math.max(1, Math.round(delay / 1000))}s…` + "`" + `);
    state.reconnectTimer = setTimeout(() => {
      state.reconnectTimer = null;
      connectWebSocket();
    }, delay);
  }

  async function makeOffer(options = {}) {
    if (state.makingOffer || !state.pc) return;
    // An offer that cannot be sent would leave us in have-local-offer with no
    // answer coming, blocking every later negotiation.
    if (!state.ws || state.ws.readyState !== WebSocket.OPEN) return;
    // A negotiation is already in flight; its answer will settle this.
    if (state.pc.signalingState !== "stable") return;
    try {
      state.makingOffer = true;
      const offer = await state.pc.createOffer(options);
      const enhancedSDP = enhanceOpusSDP(offer.sdp);
      await state.pc.setLocalDescription({ type: "offer", sdp: enhancedSDP });
      configureRtpPriorities(state.pc);
      sendMessage({
        type: "offer",
        room: state.room,
        payload: { type: "offer", sdp: enhancedSDP }
      });
    } catch (e) {
      console.error("Failed to create WebRTC offer:", e);
    } finally {
      state.makingOffer = false;
    }
  }

  function connectWebSocket() {
    if (state.ws) {
      // Detach first: the old socket's onclose would otherwise queue a second
      // reconnect on top of this one.
      state.ws.onopen = state.ws.onmessage = state.ws.onclose = state.ws.onerror = null;
      state.ws.close();
    }
    const proto = location.protocol === "https:" ? "wss:" : "ws:";
    const url = ` + "`" + `${proto}//${location.host}/ws/${encodeURIComponent(state.room)}?client_id=${encodeURIComponent(state.clientID)}` + "`" + `;
    state.ws = new WebSocket(url);

    state.ws.onopen = () => {
      // First message must be exact Join structure matching Go backend protocol
      sendMessage({
        type: "join",
        room: state.room,
        payload: {
          secret: state.secret,
          clientId: state.clientID
        }
      });
    };

    state.ws.onmessage = async ({ data }) => {
      try {
        const msg = JSON.parse(data);
        await handleSignalingMessage(msg);
      } catch (e) {
        console.error("Failed to parse signaling message:", e);
      }
    };

    state.ws.onerror = () => {
      // onclose always follows, which is where the retry is scheduled.
      console.warn("Signaling socket error");
    };

    state.ws.onclose = () => {
      if (state.intentionalClose || !state.started) return;
      // Media may still be flowing peer-to-peer; only signaling is gone.
      scheduleSignalingReconnect();
    };
  }

  function sendMessage(msg) {
    if (state.ws && state.ws.readyState === WebSocket.OPEN) {
      state.ws.send(JSON.stringify(msg));
    }
  }

  function initDraggablePip() {
    const pip = els.selfPipCard;
    if (!pip) return;

    let isDragging = false;
    let startX = 0, startY = 0;
    let startLeft = 0, startTop = 0;
    let parentRect = null;

    pip.addEventListener("pointerdown", (e) => {
      if (e.target.closest("button")) return;
      isDragging = true;
      pip.classList.add("dragging");
      pip.setPointerCapture(e.pointerId);

      const rect = pip.getBoundingClientRect();
      const parent = pip.parentElement;
      parentRect = parent.getBoundingClientRect();

      startX = e.clientX;
      startY = e.clientY;
      startLeft = rect.left - parentRect.left;
      startTop = rect.top - parentRect.top;

      pip.style.left = ` + "`" + `${startLeft}px` + "`" + `;
      pip.style.top = ` + "`" + `${startTop}px` + "`" + `;
      pip.style.right = "auto";
      pip.style.bottom = "auto";
    });

    pip.addEventListener("pointermove", (e) => {
      if (!isDragging) return;
      const dx = e.clientX - startX;
      const dy = e.clientY - startY;

      const pipWidth = pip.offsetWidth;
      const pipHeight = pip.offsetHeight;
      const maxLeft = parentRect.width - pipWidth - 8;
      const maxTop = parentRect.height - pipHeight - 8;

      const newLeft = Math.max(8, Math.min(maxLeft, startLeft + dx));
      const newTop = Math.max(8, Math.min(maxTop, startTop + dy));

      pip.style.left = ` + "`" + `${newLeft}px` + "`" + `;
      pip.style.top = ` + "`" + `${newTop}px` + "`" + `;
    });

    const endDrag = (e) => {
      if (!isDragging) return;
      isDragging = false;
      pip.classList.remove("dragging");
      try { pip.releasePointerCapture(e.pointerId); } catch (_) {}

      if (!parentRect) return;
      const pipWidth = pip.offsetWidth;
      const pipHeight = pip.offsetHeight;
      const currentLeft = parseFloat(pip.style.left) || 0;
      const currentTop = parseFloat(pip.style.top) || 0;
      const inset = 16;

      const snapToRight = currentLeft >= (parentRect.width - pipWidth) / 2;
      const snapToBottom = currentTop >= (parentRect.height - pipHeight) / 2;

      if (snapToRight) {
        pip.style.left = "auto";
        pip.style.right = ` + "`" + `${inset}px` + "`" + `;
      } else {
        pip.style.left = ` + "`" + `${inset}px` + "`" + `;
        pip.style.right = "auto";
      }

      if (snapToBottom) {
        pip.style.top = "auto";
        pip.style.bottom = ` + "`" + `${inset}px` + "`" + `;
      } else {
        pip.style.top = ` + "`" + `${inset}px` + "`" + `;
        pip.style.bottom = "auto";
      }
    };

    pip.addEventListener("pointerup", endDrag);
    pip.addEventListener("pointercancel", endDrag);
  }

  async function handleSignalingMessage(msg) {
    let payload = msg.payload;
    if (typeof payload === "string") {
      try { payload = JSON.parse(payload); } catch (_) {}
    }

    switch (msg.type) {
      case "joined": {
        state.role = payload?.role || "caller";
        state.reconnectAttempts = 0; // a completed join proves the path works
        if (payload?.participants === 2) {
          onPeerPresent();
          const media = state.pc?.connectionState;
          const mediaHealthy = media === "connected" || media === "completed";
          if (mediaHealthy) {
            // This was a signaling-only reconnect. Renegotiating here would
            // disturb a call that never actually broke.
            setStatus("Connected");
          } else {
            setStatus("Peer connected, establishing media…");
            if (els.remotePeerName) els.remotePeerName.textContent = "PEER JOINED (CONNECTING)…";
            if (state.role === "caller") makeOffer();
          }
        } else {
          setStatus("Waiting for peer to join…");
          if (els.remotePeerName) els.remotePeerName.textContent = "WAITING FOR PEER TO JOIN…";
        }
        break;
      }

      case "peer-ready":
        // The peer just (re)entered the room, so its peer connection is fresh
        // even if ours still believes it is connected. Always renegotiate.
        onPeerPresent();
        setStatus("Peer connected, establishing media…");
        if (els.remotePeerName) els.remotePeerName.textContent = "PEER JOINED (CONNECTING)…";
        if (state.role === "caller") makeOffer();
        break;

      case "offer": {
        if (!payload?.sdp || !state.pc) return;
        if (els.remotePeerName) els.remotePeerName.textContent = "PEER CONNECTING…";
        try {
          await state.pc.setRemoteDescription({ type: "offer", sdp: payload.sdp });
          await drainCandidates();
          const answer = await state.pc.createAnswer();
          const enhancedSDP = enhanceOpusSDP(answer.sdp);
          await state.pc.setLocalDescription({ type: "answer", sdp: enhancedSDP });
          configureRtpPriorities(state.pc);
          sendMessage({
            type: "answer",
            room: state.room,
            payload: { type: "answer", sdp: enhancedSDP }
          });
        } catch (e) {
          console.error("Failed to answer offer:", e);
          setStatus("Could not negotiate media, retrying…");
        }
        break;
      }

      case "answer":
        if (!payload?.sdp || !state.pc) return;
        // A late answer to a superseded offer arrives while we are back in
        // stable; applying it would throw and poison the connection.
        if (state.pc.signalingState !== "have-local-offer") {
          console.warn("Ignoring answer in state", state.pc.signalingState);
          return;
        }
        try {
          await state.pc.setRemoteDescription({ type: "answer", sdp: payload.sdp });
          if (els.remotePeerName) els.remotePeerName.textContent = "PEER CONNECTED";
          await drainCandidates();
        } catch (e) {
          console.error("Failed to apply answer:", e);
        }
        break;

      case "ice-candidate":
        if (payload?.candidate) {
          const candidateInit = {
            candidate: payload.candidate,
            sdpMid: payload.sdpMid,
            sdpMLineIndex: payload.sdpMLineIndex,
          };
          if (state.pc && state.pc.remoteDescription && state.pc.remoteDescription.type) {
            try { await state.pc.addIceCandidate(candidateInit); } catch (_) {}
          } else {
            state.pendingCandidates.push(candidateInit);
          }
        }
        break;

      case "hangup":
        // Deliberate end of call by the peer, not a network drop: the server has
        // already deleted the room, so no recovery is possible or wanted.
        state.terminal = true;
        clearTimer("reconnectTimer");
        clearTimer("disconnectTimer");
        clearTimer("peerGoneTimer");
        setStatus("Peer ended the call");
        if (els.remotePeerName) els.remotePeerName.textContent = "PEER ENDED THE CALL";
        if (els.remotePlaceholder) els.remotePlaceholder.hidden = false;
        playSound("leave");
        break;

      case "peer-left":
        // Distinct from "hangup": the peer's socket dropped, and the server
        // keeps the room for its grace window, so they may still come back.
        setStatus("Peer disconnected, waiting for them to return…");
        if (els.remotePeerName) els.remotePeerName.textContent = "PEER RECONNECTING…";
        if (els.remotePlaceholder) els.remotePlaceholder.hidden = false;
        playSound("leave");
        startPeerReturnTimer();
        break;

      case "error":
        console.warn("Signaling error received:", payload);
        if (TERMINAL_SIGNALING_CODES.has(payload?.code)) {
          // Retrying cannot fix these, and a reconnect loop would hammer the
          // server and hide the real reason from the user.
          state.terminal = true;
          clearTimer("reconnectTimer");
          clearTimer("disconnectTimer");
          clearTimer("peerGoneTimer");
          setStatus(payload?.message || "Call is no longer available");
          if (els.remotePeerName) els.remotePeerName.textContent = "CALL UNAVAILABLE";
        } else if (payload?.message) {
          setStatus(` + "`" + `Signaling error: ${payload.message}` + "`" + `);
        }
        break;
    }
  }

  async function drainCandidates() {
    for (const c of state.pendingCandidates) {
      try { await state.pc.addIceCandidate(c); } catch (_) {}
    }
    state.pendingCandidates = [];
  }

  // ==========================================
  // LIVE WEBRTC TELEMETRY MATRIX
  // ==========================================

  function startStats() {
    if (state.statsTimer) clearInterval(state.statsTimer);
    state.statsTimer = setInterval(async () => {
      if (!state.pc || state.pc.connectionState !== "connected") return;
      try {
        const stats = await state.pc.getStats();
        // Everything below is read from the live report set. Nothing is
        // substituted when a value is missing: an unknown metric renders as
        // "--" rather than a plausible-looking guess.
        let bytesSent = 0, packetsSent = 0, packetsLost = 0;
        let rtt = null, width = 0, height = 0, fps = null;
        let codec = "";
        let candidateType = "Direct P2P";

        stats.forEach(report => {
          if (report.type === "outbound-rtp" && report.kind === "video") {
            bytesSent = report.bytesSent || 0;
            packetsSent = report.packetsSent || 0;
            width = report.frameWidth || 0;
            height = report.frameHeight || 0;
            if (typeof report.framesPerSecond === "number") fps = Math.round(report.framesPerSecond);
            const codecReport = report.codecId ? stats.get(report.codecId) : null;
            if (codecReport?.mimeType) codec = codecReport.mimeType.replace(/^video\//i, "");
          }
          if (report.type === "candidate-pair" && report.state === "succeeded") {
            if (typeof report.currentRoundTripTime === "number") {
              rtt = Math.round(report.currentRoundTripTime * 1000);
            }
            const local = stats.get(report.localCandidateId);
            if (local) candidateType = ` + "`" + `${local.candidateType?.toUpperCase() || "HOST"} (${local.ip?.includes(":") ? "IPv6" : "IPv4"})` + "`" + `;
          }
          if (report.type === "remote-inbound-rtp") {
            packetsLost = report.packetsLost || 0;
          }
        });

        const now = Date.now();
        if (state.lastStatsAt > 0) {
          const bitrateMbps = (((bytesSent - state.lastBytesSent) * 8) / ((now - state.lastStatsAt) / 1000) / 1000000).toFixed(2);
          if (els.metricBitrate) els.metricBitrate.textContent = ` + "`" + `${Math.max(0, bitrateMbps)} Mbps` + "`" + `;
        }
        state.lastBytesSent = bytesSent;
        state.lastStatsAt = now;

        const rttText = rtt === null ? "--" : String(rtt);
        const resolutionText = width && height
          ? ` + "`" + `${width}x${height}${fps !== null ? ` + "`" + ` @ ${fps}` + "`" + ` : ""}` + "`" + `
          : "--";

        if (els.metricRtt) els.metricRtt.textContent = ` + "`" + `${rttText} ms` + "`" + `;
        if (els.metricLoss) {
          const lossRate = packetsSent > 0 ? ((packetsLost / packetsSent) * 100).toFixed(1) : "0.0";
          els.metricLoss.textContent = ` + "`" + `${lossRate}%` + "`" + `;
        }
        if (els.metricCandidate) els.metricCandidate.textContent = candidateType;
        if (els.metricResolution) els.metricResolution.textContent = resolutionText;
        if (els.metricCodec) els.metricCodec.textContent = codec || "--";

        if (els.callStatsBadge) {
          const parts = [];
          if (width && height) parts.push(` + "`" + `${height}P${fps !== null ? ` + "`" + ` ${fps}FPS` + "`" + ` : ""}` + "`" + `);
          if (codec) parts.push(codec.toUpperCase());
          parts.push(` + "`" + `${rttText}ms RTT` + "`" + `);
          parts.push("DTLS-SRTP");
          els.callStatsBadge.textContent = parts.join(" // ");
        }
      } catch (_) {}
    }, 1500);
  }

  function applyQualityProfile(profile) {
    if (!state.pc) return;
    const senders = state.pc.getSenders();
    const videoSender = senders.find(s => s.track && s.track.kind === "video");
    if (!videoSender || !videoSender.getParameters) return;

    const params = videoSender.getParameters();
    if (!params.encodings || params.encodings.length === 0) params.encodings = [{}];

    if (profile === "low") {
      params.encodings[0].maxBitrate = 350000;
      params.encodings[0].maxFramerate = 15;
    } else if (profile === "quality") {
      params.encodings[0].maxBitrate = 4000000;
      params.encodings[0].maxFramerate = 60;
    } else {
      delete params.encodings[0].maxBitrate;
      delete params.encodings[0].maxFramerate;
    }
    videoSender.setParameters(params).catch(() => {});
  }

  function playRemote() {
    if (!els.remoteVideo) return;
    els.remoteVideo.play().then(() => {
      if (els.playRemoteButton) els.playRemoteButton.hidden = true;
    }).catch(() => {
      if (els.playRemoteButton) els.playRemoteButton.hidden = false;
    });
  }

  function hangup(manual = false) {
    state.intentionalClose = manual;
    closeLocalResources(true);
    location.href = "/";
  }

  function closeLocalResources(leaveRoom = true) {
    if (state.callDurationTimer) clearInterval(state.callDurationTimer);
    if (state.statsTimer) clearInterval(state.statsTimer);
    clearTimer("reconnectTimer");
    clearTimer("disconnectTimer");
    clearTimer("peerGoneTimer");
    stopAudioAnalyser();
    stopPreviewMedia();

    if (state.localStream) {
      state.localStream.getTracks().forEach(t => t.stop());
      state.localStream = null;
    }
    if (state.screenStream) {
      state.screenStream.getTracks().forEach(t => t.stop());
      state.screenStream = null;
    }
    if (state.pc) {
      state.pc.close();
      state.pc = null;
    }
    if (state.ws) {
      if (leaveRoom && state.ws.readyState === WebSocket.OPEN) {
        sendMessage({ type: "leave" });
      }
      state.ws.onopen = state.ws.onmessage = state.ws.onclose = state.ws.onerror = null;
      state.ws.close();
      state.ws = null;
    }
  }

  window.addEventListener("popstate", () => {
    handleRoute();
  });

  initialize();
})();

</script>
</body>
</html>
`

const ServiceWorkerJS = `// Gocord Service Worker - Privacy-Preserving Push Notifications
"use strict";

self.addEventListener("install", () => {
  self.skipWaiting();
});

self.addEventListener("activate", (event) => {
  event.waitUntil(self.clients.claim());
});

self.addEventListener("push", (event) => {
  let data = {};
  if (event.data) {
    try {
      data = event.data.json();
    } catch (_) {
      try {
        data = { body: event.data.text() };
      } catch (__) {
        data = {};
      }
    }
  }

  const callerName = data.callerName || "";
  const title = data.title || (callerName ? ` + "`" + `Call from ${callerName}` + "`" + ` : "Incoming Call // Gocord");
  const body = data.body || (callerName ? ` + "`" + `${callerName} is calling you on Gocord` + "`" + ` : "Incoming encrypted peer-to-peer call");
  const callUrl = data.url || "/";
  const room = data.room || "";

  const options = {
    body: body,
    icon: "/assets/favicon.ico",
    badge: "/assets/favicon.ico",
    tag: room ? ` + "`" + `gocord-call-${room}` + "`" + ` : "gocord-call",
    renotify: true,
    requireInteraction: true,
    data: {
      url: callUrl,
      room: room,
      timestamp: Date.now(),
    },
    actions: [
      { action: "join", title: "✦ JOIN CALL" },
      { action: "dismiss", title: "✕ DISMISS" },
    ],
    vibrate: [200, 100, 200, 100, 400],
  };

  event.waitUntil(self.registration.showNotification(title, options));
});

self.addEventListener("notificationclick", (event) => {
  event.notification.close();

  if (event.action === "dismiss") {
    return;
  }

  const targetUrl = event.notification.data?.url || "/";

  event.waitUntil(
    self.clients.matchAll({ type: "window", includeUncontrolled: true }).then((windowClients) => {
      // If a window is already on the exact call URL, focus it
      for (const client of windowClients) {
        if (client.url === targetUrl && "focus" in client) {
          return client.focus();
        }
      }
      // If any window from this origin is open, navigate it to the call URL and focus
      if (windowClients.length > 0 && "navigate" in windowClients[0] && "focus" in windowClients[0]) {
        return windowClients[0].navigate(targetUrl).then((c) => (c ? c.focus() : null));
      }
      // Otherwise open a new window
      if (self.clients.openWindow) {
        return self.clients.openWindow(targetUrl);
      }
    })
  );
});
`
